============================================================
 CONTAINER ESCAPE AUDIT REPORT
 Generated  : Mon Jul 20 03:48:52 PM IST 2026
 Hostname   : lemmy-Inspiron-7591-2n1
 Kernel     : 6.17.0-40-generic
 UID / GID  : uid=1001(liam) gid=1001(liam) groups=1001(liam),4(adm),27(sudo),100(users)
 CGroup     : 0::/init.scope
============================================================

EXECUTIVE SUMMARY
------------------------------------------------------------
  CRITICAL:    4
  HIGH:        15
  MEDIUM:      52
  INFO:        23
  TOTAL:       94

============================================================
 CRITICAL FINDINGS
============================================================

  ID           : runtime_socket__var_run_docker.sock
  Severity     : CRITICAL
  Title        : Container runtime socket accessible: /var/run/docker.sock

  WHAT IT IS
    The container runtime's UNIX domain socket is bind-mounted into the 
    container. This socket is the administrative API for the container 
    daemon, which runs as root on the host.

  IMPACT
    Full host node compromise. An attacker uses the 
    Docker/containerd/CRI-O API to create a new privileged container with 
    the host root filesystem mounted, exec into it, and obtain a root 
    shell on the host.

  EXPLOITABILITY
    Trivial. 'docker run -v /:/host --privileged alpine chroot /host' is 
    a single command. CDK and deepce perform this automatically on socket 
    detection.

  RECOMMENDATION
    Never mount the runtime socket into application containers. For CI/CD 
    image building use rootless Docker, Kaniko, or Buildah.

  ------------------------------------------------------------

  ID           : runtime_socket__run_docker.sock
  Severity     : CRITICAL
  Title        : Container runtime socket accessible: /run/docker.sock

  WHAT IT IS
    The container runtime's UNIX domain socket is bind-mounted into the 
    container. This socket is the administrative API for the container 
    daemon, which runs as root on the host.

  IMPACT
    Full host node compromise. An attacker uses the 
    Docker/containerd/CRI-O API to create a new privileged container with 
    the host root filesystem mounted, exec into it, and obtain a root 
    shell on the host.

  EXPLOITABILITY
    Trivial. 'docker run -v /:/host --privileged alpine chroot /host' is 
    a single command. CDK and deepce perform this automatically on socket 
    detection.

  RECOMMENDATION
    Never mount the runtime socket into application containers. For CI/CD 
    image building use rootless Docker, Kaniko, or Buildah.

  ------------------------------------------------------------

  ID           : runtime_socket__run_containerd_containerd.sock
  Severity     : CRITICAL
  Title        : Container runtime socket accessible: /run/containerd/containerd.sock

  WHAT IT IS
    The container runtime's UNIX domain socket is bind-mounted into the 
    container. This socket is the administrative API for the container 
    daemon, which runs as root on the host.

  IMPACT
    Full host node compromise. An attacker uses the 
    Docker/containerd/CRI-O API to create a new privileged container with 
    the host root filesystem mounted, exec into it, and obtain a root 
    shell on the host.

  EXPLOITABILITY
    Trivial. 'docker run -v /:/host --privileged alpine chroot /host' is 
    a single command. CDK and deepce perform this automatically on socket 
    detection.

  RECOMMENDATION
    Never mount the runtime socket into application containers. For CI/CD 
    image building use rootless Docker, Kaniko, or Buildah.

  ------------------------------------------------------------

  ID           : runtime_socket__var_run_containerd_containerd.sock
  Severity     : CRITICAL
  Title        : Container runtime socket accessible: /var/run/containerd/containerd.sock

  WHAT IT IS
    The container runtime's UNIX domain socket is bind-mounted into the 
    container. This socket is the administrative API for the container 
    daemon, which runs as root on the host.

  IMPACT
    Full host node compromise. An attacker uses the 
    Docker/containerd/CRI-O API to create a new privileged container with 
    the host root filesystem mounted, exec into it, and obtain a root 
    shell on the host.

  EXPLOITABILITY
    Trivial. 'docker run -v /:/host --privileged alpine chroot /host' is 
    a single command. CDK and deepce perform this automatically on socket 
    detection.

  RECOMMENDATION
    Never mount the runtime socket into application containers. For CI/CD 
    image building use rootless Docker, Kaniko, or Buildah.

  ------------------------------------------------------------
============================================================
 HIGH FINDINGS
============================================================

  ID           : kh_unprivileged_userns
  Severity     : HIGH
  Title        : Unprivileged user namespace creation is enabled — exposes significant kernel attack surface

  WHAT IT IS
    Kernel parameter: kernel.unprivileged_userns_clone=1. Unprivileged 
    user namespace creation is enabled. Any unprivileged user can call 
    unshare(CLONE_NEWUSER) to create a new user namespace with their own 
    UID mappings, gaining access to capabilities within that namespace 
    and the ability to create further namespaces.

  IMPACT
    User namespaces dramatically expand the kernel attack surface 
    accessible to unprivileged users. The following attack classes are 
    only possible with user namespace access: (1) The majority of 
    container escape CVEs since 2019, including cgroup release_agent 
    escapes, overlayfs vulnerabilities, and namespace confusion attacks. 
    (2) Copy Fail (CVE-2026-31431) is significantly easier to exploit 
    with user namespace access. (3) Dirty Frag 
    (CVE-2026-43284/CVE-2026-43500) uses standard sockets that are more 
    accessible within user namespaces. (4) eBPF unprivileged access 
    (check 28) is most dangerous when combined with user namespace 
    creation. (5) The runc trilogy (CVE-2025-31133/-52565/-52881, check 
    26) is exploitable by anyone who can spawn containers — user 
    namespaces make self-hosting containers feasible without root. 
    Google, Red Hat, and Canonical all restrict or disable unprivileged 
    user namespaces in hardened deployments.

  EXPLOITABILITY
    High. An unprivileged container user with network access can use user 
    namespaces to access capabilities (CAP_NET_RAW, CAP_NET_BIND_SERVICE) 
    within the namespace, enabling a wide range of subsequent attacks. 
    Many published container escape PoCs begin with an 
    unshare(CLONE_NEWUSER) call.

  RECOMMENDATION
    1) On Debian/Ubuntu: set kernel.unprivileged_userns_clone=0 in 
    /etc/sysctl.d/ if rootless containers are not required. 2) On 
    RHEL/Fedora: set user.max_user_namespaces=0. 3) If rootless 
    containers are required, restrict user namespace creation to specific 
    users/groups via AppArmor or seccomp. 4) Ubuntu 24.04+ (Noble) and 
    Debian 12+ support restricting user namespace creation per-process 
    via AppArmor's 'userns' rule — use this as a middle ground.

  ------------------------------------------------------------

  ID           : kh_mod_binfmt_misc
  Severity     : HIGH
  Title        : Dangerous kernel module loaded: binfmt_misc

  WHAT IT IS
    Kernel module 'binfmt_misc' is currently loaded (visible in 
    /proc/modules). Reason: Registers additional binary format 
    interpreters via a filesystem interface; if writable from a 
    container, allows registering a handler that runs as root on the host 
    when a container binary is executed. Associated CVE(s): none.

  IMPACT
    Module-specific attack surface is active and exploitable. See the 
    reason field above. For modules with active ITW CVEs (algif_aead, 
    esp4, esp6, rxrpc), the host is exposed to current in-the-wild 
    exploits providing local privilege escalation and container escape.

  EXPLOITABILITY
    Module is already loaded — no auto-load trigger needed. Exploit 
    primitives (sockets, syscalls) are immediately accessible. Attack 
    complexity depends on the specific module — see reasons above.

  RECOMMENDATION
    Ensure /proc/sys/fs/binfmt_misc is mounted read-only or not at all in 
    container contexts.

  ------------------------------------------------------------

  ID           : kh_mod_vsock
  Severity     : HIGH
  Title        : [CVE-2025-21756] Dangerous kernel module loaded: vsock

  WHAT IT IS
    Kernel module 'vsock' is currently loaded (visible in /proc/modules). 
    Reason: Virtio/VM sockets core (AF_VSOCK) — host-guest socket 
    transport; 'Attack of the Vsock' (CVE-2025-21756) is a use-after-free 
    in this subsystem giving local privilege escalation. Rarely needed 
    inside application containers.. Associated CVE(s): CVE-2025-21756.

  IMPACT
    Module-specific attack surface is active and exploitable. See the 
    reason field above. For modules with active ITW CVEs (algif_aead, 
    esp4, esp6, rxrpc), the host is exposed to current in-the-wild 
    exploits providing local privilege escalation and container escape.

  EXPLOITABILITY
    Module is already loaded — no auto-load trigger needed. Exploit 
    primitives (sockets, syscalls) are immediately accessible. Attack 
    complexity depends on the specific module — see reasons above.

  RECOMMENDATION
    Blacklist if AF_VSOCK is not required (it usually is not in 
    containers): 'install vsock /bin/false'. Patch the kernel for 
    CVE-2025-21756.

  ------------------------------------------------------------

  ID           : kh_mod_nf_conntrack_netlink
  Severity     : HIGH
  Title        : Dangerous kernel module loaded: nf_conntrack_netlink

  WHAT IT IS
    Kernel module 'nf_conntrack_netlink' is currently loaded (visible in 
    /proc/modules). Reason: Provides a Netlink interface for querying and 
    modifying connection tracking state; exploited in multiple container 
    network escape scenarios. Associated CVE(s): none.

  IMPACT
    Module-specific attack surface is active and exploitable. See the 
    reason field above. For modules with active ITW CVEs (algif_aead, 
    esp4, esp6, rxrpc), the host is exposed to current in-the-wild 
    exploits providing local privilege escalation and container escape.

  EXPLOITABILITY
    Module is already loaded — no auto-load trigger needed. Exploit 
    primitives (sockets, syscalls) are immediately accessible. Attack 
    complexity depends on the specific module — see reasons above.

  RECOMMENDATION
    Blacklist if not required for NAT/firewall functionality: 'install 
    nf_conntrack_netlink /bin/false'.

  ------------------------------------------------------------

  ID           : io_uring_exposure
  Severity     : HIGH
  Title        : io_uring is reachable from this container (attack surface for CVE-2026-43121 and the io_uring LPE class)

  WHAT IT IS
    io_uring_setup(2) succeeded from within this context, so the io_uring 
    interface is reachable by unprivileged code here. 
    kernel.io_uring_disabled=0 (0=enabled for all, 1=disabled for 
    unprivileged, 2=disabled for all; 'unset' means the sysctl is absent 
    on this kernel). io_uring has a long history of kernel LPE and 
    container-escape primitives; the most recent in this database is 
    CVE-2026-43121 (zcrx freelist out-of-bounds write, fixed in stable 
    6.18.16).

  IMPACT
    io_uring reachability is a prerequisite for io_uring-based exploits. 
    On an unpatched kernel, a reachable io_uring (and, for CVE-2026-43121 
    specifically, reachable zcrx zero-copy receive on an SMP host) can be 
    leveraged for out-of-bounds kernel writes and privilege escalation. 
    Even patched, io_uring substantially enlarges the 
    unprivileged-reachable kernel attack surface.

  EXPLOITABILITY
    Reachability only — this probe does NOT attempt exploitation and 
    does not confirm an unpatched kernel. Combine with the CVE engine's 
    kernel-version result for CVE-2026-43121 to assess actual 
    exploitability.

  RECOMMENDATION
    1) If workloads do not require io_uring, disable it for unprivileged 
    tasks: set kernel.io_uring_disabled=2 (all) or =1 (unprivileged) via 
    /etc/sysctl.d/. 2) Alternatively block io_uring_setup(2), 
    io_uring_enter(2), and io_uring_register(2) in the container seccomp 
    profile (the RuntimeDefault profile blocks io_uring on recent 
    runtimes). 3) Patch the kernel to >= 6.18.16 (or your distro 
    backport) for CVE-2026-43121. 4) Disable zero-copy receive (zcrx) 
    reachability where not needed.

  ------------------------------------------------------------

  ID           : cve_cve_2026_31431_crypto_af_alg
  Severity     : HIGH
  Title        : Copy Fail (CVE-2026-31431) — HIGH [ITW] [CISA-KEV]

  WHAT IT IS
    CVE: CVE-2026-31431. Name: Copy Fail. CVSS: 7.8. Kernel: 
    6.17.0-40-generic. Detection context: kernel 6.17.0-40-generic 
    confirmed in affected range; socket AF=38 family is reachable, but 
    module(s) algif_aead are confirmed blacklisted in /etc/modprobe.d — 
    this blocks auto-load of the specific vulnerable transform at 
    request_module() time, though this check cannot independently confirm 
    the blacklist covers every code path the socket check's family-level 
    probe does not exercise (e.g. bind()-time algorithm lookup). ITW: 
    yes. Public PoC: yes. CISA KEV: yes. Subsystem: crypto/af_alg. 
    Modules: algif_aead. Copy Fail (CVE-2026-31431) is a logic flaw in 
    the Linux kernel's AEAD socket interface (algif_aead, part of the 
    AF_ALG userspace crypto API). Improper handling of scatter-gather 
    lists in the AEAD fast path allows any unprivileged user to perform a 
    controlled 4-byte write into the page cache of any readable file on 
    the system. Discovered by Xint. Mainline fix: revert of a 2017 
    optimisation (commit a664bf3d603d).

  CHECKS PERFORMED
    [FLAG] Version verdict (enriched): distro_status: ubuntu/24.04 marked 
        vulnerable (no fix in this package line)
    [pass] Module loaded (/proc/modules): none of [algif_aead] present in 
        /proc/modules
    [pass] Module blacklist (/etc/modprobe.d): all of [algif_aead] 
        blacklisted via 'install ... /bin/false'
    [FLAG] Socket reachability: AF=38 SOCK=5 PROTO=0: opened cleanly (AF 
        reachable)
    [FLAG] Severity synthesis: kernel affected; socket family reachable 
        BUT module(s) confirmed blacklisted (algif_aead) -> HIGH, not CRITICAL 
        — socket check only confirms family-level reachability (e.g. AF_ALG 
        core), not that the specific blacklisted transform can load; blacklist is 
        a real but not fully-verified-at-this-precision mitigation

  IMPACT
    Privilege escalation to root for any local user; container escape 
    when the page cache is shared across containers sharing the same host 
    kernel. A single ~732-byte Python script achieves reliable root on 
    Ubuntu, RHEL, Debian, Amazon Linux, and SUSE without race conditions. 
    Because page cache is shared across all containers on a node, a 
    compromised container can corrupt binaries used by privileged 
    containers or the host itself.

  EXPLOITABILITY
    Trivial. A public 732-byte Python PoC (no dependencies beyond 
    standard library) achieves root reliably. CVSS 7.8. No capabilities 
    required — any unprivileged user inside the container can exploit 
    this. Deterministic; no race condition. Works on any kernel from 4.14 
    to the patch boundary.

  RECOMMENDATION
    1) Apply your distribution's kernel update immediately. 2) Interim: 
    unload algif_aead module and blacklist it: 'rmmod algif_aead && echo 
    install algif_aead /bin/false > /etc/modprobe.d/copyfail.conf'. RHEL 
    9 note: algif_aead may be built-in — modprobe blacklist has no 
    effect; use 'initcall_blacklist=algif_aead_init' as a kernel boot 
    parameter. 3) Block AF_ALG socket creation via seccomp if not 
    required by workloads (CERT-EU recommendation regardless of patch 
    status). Interim mitigation (compensating control until patched): 
    rmmod algif_aead 2>/dev/null; echo 'install algif_aead /bin/false' > 
    /etc/modprobe.d/copyfail.conf Patch status: comparison was made 
    against recorded fixed versions (5.15:5.15.182 5.10:5.10.238 
    6.1:6.1.138 6.6:6.6.88 6.12:6.12.24 6.13:6.13.0 mainline:6.18.22). 
    Distributions often backport fixes without changing the upstream 
    version string, so confirm against your distribution's security 
    advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_43284_net_xfrm_esp
  Severity     : HIGH
  Title        : Dirty Frag ESP (CVE-2026-43284) — HIGH [ITW]

  WHAT IT IS
    CVE: CVE-2026-43284. Name: Dirty Frag ESP. CVSS: 8.8. Kernel: 
    6.17.0-40-generic. Detection context: kernel 6.17.0-40-generic 
    version unknown for this distro/release — no matching 
    distro_status/vendor_defer row; treated conservatively as affected, 
    NOT a confirmed range hit; socket AF=16 family is reachable, but 
    module(s) esp4 esp6 are confirmed blacklisted in /etc/modprobe.d — 
    this blocks auto-load of the specific vulnerable transform at 
    request_module() time, though this check cannot independently confirm 
    the blacklist covers every code path the socket check's family-level 
    probe does not exercise (e.g. bind()-time algorithm lookup). ITW: 
    yes. Public PoC: yes. CISA KEV: no. Subsystem: net/xfrm/esp. Modules: 
    esp4 esp6. Dirty Frag (CVE-2026-43284) is the IPsec ESP component of 
    the Dirty Frag vulnerability chain, disclosed May 2026 by Hyunwoo 
    Kim. The xfrm-ESP receive fast path performs in-place decryption over 
    paged fragments not privately owned by the kernel (pipe pages 
    attached via splice/sendfile/MSG_SPLICE_PAGES). This allows a local 
    user to write arbitrary attacker-controlled plaintext at any chosen 
    offset in the page cache in a single deterministic operation. 
    Mainline fix: commit f4c50a4034e6.

  CHECKS PERFORMED
    [info] Version verdict (enriched): no matching fixed-version data for 
        ubuntu/24.04 flavour=generic (running 6.17.0-40-generic); cannot assert 
        patched — verify against vendor tracker. Heuristic only (base kernel 
        version vs NVD upstream ranges, NOT distro-backport-aware): vanilla 
        6.17.0 in NVD affected range [6.13, 6.18.28) — upstream fix at 6.18.28. 
        Distributions routinely backport fixes without changing the upstream 
        version string, so this heuristic can be wrong in either direction — it 
        is supporting context only, never a substitute for the vendor advisory.
    [pass] Module loaded (/proc/modules): none of [esp4 esp6] present in 
        /proc/modules
    [pass] Module blacklist (/etc/modprobe.d): all of [esp4 esp6] 
        blacklisted via 'install ... /bin/false'
    [FLAG] Socket reachability: AF=16 SOCK=3 PROTO=6: opened cleanly (AF 
        reachable)
    [FLAG] Severity synthesis: kernel affected; socket family reachable 
        BUT module(s) confirmed blacklisted (esp4 esp6) -> HIGH, not CRITICAL — 
        socket check only confirms family-level reachability (e.g. AF_ALG core), 
        not that the specific blacklisted transform can load; blacklist is a real 
        but not fully-verified-at-this-precision mitigation

  IMPACT
    Local privilege escalation to root and container escape. Provides 
    full attacker-controlled page cache writes (superior to Copy Fail's 
    4-byte write). Works even if Copy Fail (algif_aead) has been 
    mitigated. Two CVEs are typically chained: esp4/esp6 handles the 
    reliable write primitive, rxrpc handles edge cases. Active 
    in-the-wild exploitation confirmed on Ubuntu 24.04, RHEL 10, Debian 
    12, Amazon Linux 2023, SUSE 16.

  EXPLOITABILITY
    High. Deterministic — no race window. Requires only standard 
    syscalls: socket, setsockopt, bind, vmsplice, splice, sendmsg. No 
    unusual privileges needed beyond what is available to any 
    unprivileged container user. Working exploit published same day as 
    disclosure (May 2026). CVSS 8.8.

  RECOMMENDATION
    1) Apply kernel patch immediately: patched in 6.18.25+, 6.19.14+, or 
    7.0+. 2) Interim: blacklist esp4 and esp6 — see the 
    compensating-control command appended below (from this entry's 
    mitigation field). WARNING: disabling esp4/esp6 breaks IPsec/VPN 
    tunnels — evaluate operational impact before fleet-wide deployment. 
    3) If IPsec is in use, prioritise kernel patching over module 
    blacklisting. Interim mitigation (compensating control until 
    patched): rmmod esp4 esp6 2>/dev/null; printf 'install esp4 
    /bin/false\ninstall esp6 /bin/false\n' > 
    /etc/modprobe.d/dirtyfrag.conf Patch status: comparison was made 
    against recorded fixed versions (5.15:5.15.185 6.1:6.1.141 6.6:6.6.92 
    6.12:6.12.29 mainline:6.18.25). Distributions often backport fixes 
    without changing the upstream version string, so confirm against your 
    distribution's security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_43500_net_rxrpc
  Severity     : HIGH
  Title        : Dirty Frag RxRPC (CVE-2026-43500) — HIGH [ITW]

  WHAT IT IS
    CVE: CVE-2026-43500. Name: Dirty Frag RxRPC. CVSS: 7.8. Kernel: 
    6.17.0-40-generic. Detection context: kernel 6.17.0-40-generic 
    version unknown for this distro/release — no matching 
    distro_status/vendor_defer row; treated conservatively as affected, 
    NOT a confirmed range hit; socket AF=35 family is reachable, but 
    module(s) rxrpc are confirmed blacklisted in /etc/modprobe.d — this 
    blocks auto-load of the specific vulnerable transform at 
    request_module() time, though this check cannot independently confirm 
    the blacklist covers every code path the socket check's family-level 
    probe does not exercise (e.g. bind()-time algorithm lookup). ITW: 
    yes. Public PoC: yes. CISA KEV: no. Subsystem: net/rxrpc. Modules: 
    rxrpc. Dirty Frag (CVE-2026-43500) is the RxRPC component of the 
    Dirty Frag chain. The RxRPC receive path (used by the AFS filesystem 
    client protocol) performs in-place decryption over pages it does not 
    privately own, enabling the same page cache write primitive as 
    CVE-2026-43284 but through a different code path. The two CVEs are 
    chained: together they cover each other's edge cases, providing a 
    highly reliable exploit primitive. Mainline fix: commit aa54b1d27fe0.

  CHECKS PERFORMED
    [info] Version verdict (enriched): no matching fixed-version data for 
        ubuntu/24.04 flavour=generic (running 6.17.0-40-generic); cannot assert 
        patched — verify against vendor tracker. Heuristic only (base kernel 
        version vs NVD upstream ranges, NOT distro-backport-aware): vanilla 
        6.17.0 in NVD affected range [5.3, 6.18.29) — upstream fix at 6.18.29. 
        Distributions routinely backport fixes without changing the upstream 
        version string, so this heuristic can be wrong in either direction — it 
        is supporting context only, never a substitute for the vendor advisory.
    [pass] Module loaded (/proc/modules): none of [rxrpc] present in 
        /proc/modules
    [pass] Module blacklist (/etc/modprobe.d): all of [rxrpc] blacklisted 
        via 'install ... /bin/false'
    [FLAG] Socket reachability: AF=35 SOCK=5 PROTO=0: errno 1 (EPERM) — 
        AF reachable
    [FLAG] Severity synthesis: kernel affected; socket family reachable 
        BUT module(s) confirmed blacklisted (rxrpc) -> HIGH, not CRITICAL — 
        socket check only confirms family-level reachability (e.g. AF_ALG core), 
        not that the specific blacklisted transform can load; blacklist is a real 
        but not fully-verified-at-this-precision mitigation

  IMPACT
    Same class as CVE-2026-43284. The RxRPC path is specifically needed 
    to handle the edge cases the ESP path cannot reach, making the 
    combined exploit significantly more reliable than either CVE alone. 
    As of May 2026, no distro has shipped a patch for CVE-2026-43500 — 
    module blacklisting (rxrpc) is the ONLY mitigation available on most 
    systems.

  EXPLOITABILITY
    High when combined with CVE-2026-43284. CVSS 7.8. Module auto-loads 
    on AF_RXRPC socket creation. An attacker who has applied the 
    esp4/esp6 blacklist but not rxrpc remains vulnerable through this 
    path.

  RECOMMENDATION
    1) Blacklist rxrpc immediately: 'rmmod rxrpc 2>/dev/null; echo 
    install rxrpc /bin/false > /etc/modprobe.d/dirtyfrag-rxrpc.conf'. 
    NOTE: this breaks AFS filesystem client if in use — verify before 
    deploying. 2) Apply kernel patch when available. 3) Even if rxrpc is 
    not currently loaded, the blacklist is required as the module 
    auto-loads on socket creation. Interim mitigation (compensating 
    control until patched): rmmod rxrpc 2>/dev/null; echo 'install rxrpc 
    /bin/false' > /etc/modprobe.d/dirtyfrag-rxrpc.conf Patch status: 
    comparison was made against recorded fixed versions (5.15:5.15.185 
    6.1:6.1.141 6.6:6.6.92 6.12:6.12.29 mainline:6.18.25). Distributions 
    often backport fixes without changing the upstream version string, so 
    confirm against your distribution's security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_46243_fs_smb_client_cifs_spnego
  Severity     : HIGH
  Title        : CIFSwitch (CVE-2026-46243) — HIGH

  WHAT IT IS
    CVE: CVE-2026-46243. Name: CIFSwitch. CVSS: 7.8. Kernel: 
    6.17.0-40-generic. Detection context: kernel 6.17.0-40-generic 
    version unknown for this distro/release — no matching 
    distro_status/vendor_defer row; treated conservatively as affected, 
    NOT a confirmed range hit; modules not currently loaded but not 
    blacklisted — auto-load on socket creation is possible: cifs. ITW: 
    no. Public PoC: yes. CISA KEV: no. Subsystem: 
    fs/smb/client/cifs_spnego. Modules: cifs. CIFSwitch (CVE-2026-46243) 
    is a local privilege escalation discovered by Asim Manizada and 
    disclosed on oss-security on 2026-05-28 after a linux-distros 
    embargo. The bug has been present in the kernel since 2007. The CIFS 
    kernel client registers the cifs.spnego key type without validating 
    that key creation requests originate from kernel CIFS — the 
    .vet_description hook is absent from the cifs_spnego_key_type 
    definition. Any unprivileged process can call 
    request_key("cifs.spnego", forged_description, "") with 
    attacker-controlled fields including pid, uid, creduid, and 
    upcall_target. Because the default 
    /etc/request-key.d/cifs.spnego.conf rule launches cifs.upcall as root 
    for any cifs.spnego key request, the rootful helper runs with 
    attacker-supplied parameters. cifs.upcall parses upcall_target=app 
    and switches into the namespace of the attacker-supplied pid before 
    performing an NSS getpwuid() lookup — in the attacker's mount 
    namespace. This allows loading an attacker-controlled NSS module as 
    root. Upstream fix: commit 3da1fdf4efbc adds .vet_description to 
    cifs_spnego_key_type to reject descriptions not originating from 
    kernel CIFS. CVE assigned 2026-06-01.

  CHECKS PERFORMED
    [info] Version verdict (enriched): no matching fixed-version data for 
        ubuntu/24.04 flavour=generic (running 6.17.0-40-generic); cannot assert 
        patched — verify against vendor tracker. Heuristic only (base kernel 
        version vs NVD upstream ranges, NOT distro-backport-aware): vanilla 
        6.17.0 in NVD affected range [6.13, 6.18.34) — upstream fix at 6.18.34. 
        Distributions routinely backport fixes without changing the upstream 
        version string, so this heuristic can be wrong in either direction — it 
        is supporting context only, never a substitute for the vendor advisory.
    [pass] Module loaded (/proc/modules): none of [cifs] present in 
        /proc/modules
    [FLAG] Module blacklist (/etc/modprobe.d): NOT blacklisted: cifs — 
        auto-load on use remains possible
    [FLAG] Severity synthesis: kernel affected; modules not loaded but 
        not blacklisted (cifs) — auto-load possible -> HIGH

  IMPACT
    Any unprivileged local user achieves a root shell in a single command 
    on affected systems. The exploit is reliable and deterministic — no 
    race condition, no brute force. Impact is bounded by three 
    prerequisites: unpatched kernel, cifs-utils installed with default 
    request-key rules, and unprivileged user namespaces enabled. On 
    systems meeting all three conditions, the author confirmed 
    exploitability across approximately 30 distribution and edition 
    combinations including RHEL 8/9/10, Ubuntu 22.04/24.04, Debian 12, 
    SUSE 15, Amazon Linux 2023, and Oracle Linux. In container 
    environments where cifs-utils is present in the image and user 
    namespaces are available, any container user can exploit this to 
    obtain root on the host kernel.

  EXPLOITABILITY
    High on affected systems. Public PoC published by the researcher on 
    2026-05-28 (github.com/manizada/CIFSwitch) simultaneously with the 
    oss-security advisory. Single command to root. CVSS 7.8. The 
    vulnerability is not universal — exploitability requires cifs-utils 
    with default config and unprivileged user namespaces. SELinux in 
    enforcing mode and AppArmor with cifs.upcall confinement are 
    effective mitigations at the userspace layer even on an unpatched 
    kernel.

  RECOMMENDATION
    1) Apply kernel patch: upstream fix is commit 3da1fdf4efbc, queued 
    for stable. Apply your distribution's kernel update once available. 
    Check: RHEL/CentOS/AlmaLinux/Rocky: 'yum update kernel'; 
    Ubuntu/Debian: 'apt-get update && apt-get upgrade linux-image-$(uname 
    -r)'; SUSE: 'zypper update kernel-default'. 2) Interim mitigations 
    (choose based on operational impact): a) Remove cifs-utils if SMB 
    mounts are not required: 'apt-get remove cifs-utils' or 'yum remove 
    cifs-utils'. b) Disable the cifs.spnego request-key rule: 'rm 
    /etc/request-key.d/cifs.spnego.conf'. This breaks 
    Kerberos-authenticated CIFS mounts but is otherwise safe. c) Block 
    the cifs module: 'rmmod cifs 2>/dev/null && echo install cifs 
    /bin/false > /etc/modprobe.d/cifswitch.conf'. WARNING: breaks all 
    CIFS/SMB mounts. d) Disable unprivileged user namespaces (check 44 in 
    this audit): 'sysctl -w kernel.unprivileged_userns_clone=0'. This 
    removes the namespace pivot step and breaks the chain without 
    affecting CIFS mounts. 3) On SELinux systems: ensure SELinux is in 
    enforcing mode — this confines cifs.upcall even when invoked with 
    attacker-supplied parameters. 4) Audit for exploitation attempts: 
    check audit logs for request_key calls with cifs.spnego key 
    descriptions originating from non-kernel processes ('ausearch -k 
    keyring_monitor'). Interim mitigation (compensating control until 
    patched): rmmod cifs 2>/dev/null; echo 'install cifs /bin/false' > 
    /etc/modprobe.d/cifswitch.conf Patch status: no fixed kernel version 
    is recorded for this CVE (fixed_versions=none) — at disclosure no 
    upstream fix was available, or this is a userspace/runtime CVE 
    tracked separately. This finding cannot confirm a patched kernel; 
    verify against your distribution's security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_46300_net_core_skbuff
  Severity     : HIGH
  Title        : Fragnesia ESP (CVE-2026-46300) — HIGH

  WHAT IT IS
    CVE: CVE-2026-46300. Name: Fragnesia ESP. CVSS: 7.8. Kernel: 
    6.17.0-40-generic. Detection context: kernel 6.17.0-40-generic 
    version unknown for this distro/release — no matching 
    distro_status/vendor_defer row; treated conservatively as affected, 
    NOT a confirmed range hit; modules not currently loaded but not 
    blacklisted — auto-load on socket creation is possible: espintcp. 
    ITW: no. Public PoC: yes. CISA KEV: no. Subsystem: net/core/skbuff. 
    Modules: esp4 esp6 espintcp. Fragnesia (CVE-2026-46300) is a 
    DirtyFrag-family local privilege escalation disclosed 2026-05-13 by 
    William Bowling / V12 Security. The root defect is in core skbuff: 
    skb_try_coalesce() can attach paged frags from one skb to another but 
    loses the SKBFL_SHARED_FRAG marker. ESP input checks 
    skb_has_shared_frag() before deciding whether a nonlinear skb can 
    skip skb_cow_data(); if TCP receive coalescing moved shared frags 
    into an unmarked skb, ESP decrypts in place over page-cache-backed 
    frags, letting an unprivileged user write into the page cache of a 
    root-owned read-only file. Emerged as an unintended side effect of 
    the Dirty Frag fix (f4c50a4034e6), which first made the SHARED_FRAG 
    bit actually checked at decrypt time. Upstream fix commit 
    f84eca581739.

  CHECKS PERFORMED
    [info] Version verdict (enriched): no matching fixed-version data for 
        ubuntu/24.04 flavour=generic (running 6.17.0-40-generic); cannot assert 
        patched — verify against vendor tracker. Heuristic only (base kernel 
        version vs NVD upstream ranges, NOT distro-backport-aware): vanilla 
        6.17.0 in NVD affected range [6.13, 6.18.33) — upstream fix at 6.18.33. 
        Distributions routinely backport fixes without changing the upstream 
        version string, so this heuristic can be wrong in either direction — it 
        is supporting context only, never a substitute for the vendor advisory.
    [pass] Module loaded (/proc/modules): none of [esp4 esp6 espintcp] 
        present in /proc/modules
    [FLAG] Module blacklist (/etc/modprobe.d): NOT blacklisted: espintcp; 
        blacklisted: esp4 esp6 — auto-load on use remains possible
    [FLAG] Severity synthesis: kernel affected; modules not loaded but 
        not blacklisted (espintcp) — auto-load possible -> HIGH

  IMPACT
    Local privilege escalation to root by page-cache poisoning of a 
    setuid binary; on-disk file unchanged so integrity tooling misses it. 
    Container-escape / node-compromise vector on multi-tenant hosts. 
    Kernels that already received the Dirty Frag fix remain vulnerable 
    until this and the DirtyClone follow-up are also applied — the full 
    DirtyFrag-family patch set is required for protection.

  EXPLOITABILITY
    High. Public PoC (github.com/v12-security/pocs, fragnesia). 
    Deterministic, no race condition. The PoC requires 
    CONFIG_INET_ESPINTCP (espintcp) to reach the bug, so kernels built 
    without it block this specific exploit, though the underlying skbuff 
    defect may be reachable via other paths. Reachable by unprivileged 
    users via namespace-local CAP_NET_ADMIN where unprivileged user 
    namespaces are enabled.

  RECOMMENDATION
    Apply the distribution kernel update (Debian Bullseye 5.10.257-1, 
    Bookworm 6.1.174-1, Trixie 6.12.90-2; AlmaLinux 8/9/10 patched 
    kernels per almalinux.org advisory; upstream fix commit f84eca581739) 
    and REBOOT. Interim: the Dirty Frag esp4/esp6 blacklist also blunts 
    this; additionally blacklist espintcp, and/or disable unprivileged 
    user namespaces. WARNING: blacklisting breaks IPsec/VPN. Verify 
    operational impact first. Interim mitigation (compensating control 
    until patched): printf 'install esp4 /bin/false\ninstall esp6 
    /bin/false\ninstall espintcp /bin/false\n' > 
    /etc/modprobe.d/fragnesia.conf; rmmod espintcp esp4 esp6 2>/dev/null 
    Patch status: comparison was made against recorded fixed versions 
    (debian-bullseye:5.10.257-1 debian-bookworm:6.1.174-1 
    debian-trixie:6.12.90-2 debian-sid:7.0.12-2 mainline:7.1-rc4). 
    Distributions often backport fixes without changing the upstream 
    version string, so confirm against your distribution's security 
    advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_41326_kata-containers_kata-agent_CopyFile
  Severity     : HIGH
  Title        : Kata CopyFile Symlink Subversion (CVE-2026-41326) — HIGH

  WHAT IT IS
    CVE: CVE-2026-41326. Name: Kata CopyFile Symlink Subversion. CVSS: 
    8.2. Kernel: 6.17.0-40-generic. Detection context: manual/advisory 
    tracking entry — no kernel version/module test applies; affected 
    component: kata-containers; fixed in kata-containers: 3.29.0. ITW: 
    no. Public PoC: yes. CISA KEV: no. Subsystem: 
    kata-containers/kata-agent CopyFile. Modules: none. CVE-2026-41326 is 
    an authorization-bypass and symlink-following vulnerability (CWE-61) 
    in the CopyFile API of the Kata Containers kata-agent (the guest-side 
    ttrpc/gRPC interface the host shim uses to manage container lifecycle 
    and transfer files). The agent's Rego/OPA policy engine validates the 
    destination path of a write but ignores the file type and payload. An 
    attacker with host-level access to the kata-agent socket first issues 
    a CopyFile request creating a symlink in an allowed directory (e.g. 
    /run/kata-containers/shared/containers/) pointing to a restricted 
    guest file, then issues a second CopyFile write to that symlink, 
    which the agent blindly follows, overwriting the target. Affects Kata 
    3.4.0 through 3.28.0; fixed in 3.29.0.

  CHECKS PERFORMED
    [skip] Automated detection: primary detection is behavioural / 
        component-version based; tracked as advisory at severity HIGH; 
        component=kata-containers; component fixed=3.29.0 — confirm via 
        dedicated check / installed component version

  IMPACT
    A compromised or malicious host can write to arbitrary locations 
    inside the guest workload image — overwriting guest binaries (e.g. 
    /bin/bash), injecting code, altering execution flow, or exfiltrating 
    data. The impact is most severe for Confidential VMs (CVMs), whose 
    entire security model assumes the guest is isolated from an untrusted 
    host; this flaw breaks that boundary. It is a host-to-guest integrity 
    violation rather than a guest-to-host escape.

  EXPLOITABILITY
    Public technical writeups and a Python ttrpc/gRPC PoC exist (two 
    CopyFile calls: create symlink, then write through it). Requires 
    host-side access to the kata-agent communication socket — i.e. an 
    already-compromised or malicious host in the CVM threat model. CVSS 
    4.0 base 8.2 (VC:N/VI:H/SC:H).

  RECOMMENDATION
    1) Upgrade Kata Containers to >= 3.29.0 (also shipped in OpenShift 
    4.19.34 and equivalent vendor releases). 2) Restrict and authenticate 
    access to the kata-agent ttrpc/gRPC socket; ensure only the trusted 
    shim can reach it. 3) Where a custom agent policy is used, validate 
    symlink targets and reject CopyFile writes whose resolved path 
    escapes the permitted directory. 4) For CVM deployments, treat any 
    pre-3.29.0 agent as integrity-compromised. The script 
    check_kata_agent_socket flags the presence/accessibility of the 
    kata-agent socket from this context. Interim mitigation (compensating 
    control until patched): Upgrade Kata Containers to >= 3.29.0; 
    restrict access to the kata-agent ttrpc/gRPC socket; add a policy 
    that validates symlink targets Patch status: no fixed kernel version 
    is recorded for this CVE (fixed_versions=none) — at disclosure no 
    upstream fix was available, or this is a userspace/runtime CVE 
    tracked separately. This finding cannot confirm a patched kernel; 
    verify against your distribution's security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_46680_containerd_user-id_handling
  Severity     : HIGH
  Title        : containerd runAsNonRoot Bypass (CVE-2026-46680) — HIGH

  WHAT IT IS
    CVE: CVE-2026-46680. Name: containerd runAsNonRoot Bypass. CVSS: 7.3. 
    Kernel: 6.17.0-40-generic. Detection context: manual/advisory 
    tracking entry — no kernel version/module test applies; affected 
    component: containerd; fixed in containerd: 1.7.32. ITW: no. Public 
    PoC: no. CISA KEV: no. Subsystem: containerd user-id handling. 
    Modules: none. CVE-2026-46680 is an input-validation bug in 
    containerd. When a container is launched with a numeric User 
    directive that cannot be parsed as a 32-bit integer (e.g. a value 
    larger than 2147483647), containerd incorrectly treats the numeric 
    string as a username and resolves it against the container image's 
    /etc/passwd. A crafted image that maps that large numeric string to 
    root (UID 0) causes the container to ultimately run as root. The 
    closely related earlier advisory is CVE-2024-40635 (same class, UID 
    overflow / wraparound).

  CHECKS PERFORMED
    [skip] Automated detection: primary detection is behavioural / 
        component-version based; tracked as advisory at severity HIGH; 
        component=containerd; component fixed=1.7.32 — confirm via dedicated 
        check / installed component version

  IMPACT
    Complete bypass of the Kubernetes runAsNonRoot security control: a 
    workload that is supposed to be forced non-root runs as UID 0 
    instead. Combined with any other in-container-root escalation path 
    this becomes a force multiplier toward container escape and host 
    compromise. Requires the ability to run a crafted/untrusted image (or 
    a malicious image author).

  EXPLOITABILITY
    No public exploit code; the mechanism is straightforward (image USER 
    set to e.g. 4294967296 plus a matching /etc/passwd entry mapping it 
    to root). Moderate severity; gated on running untrusted images and on 
    the cluster relying on runAsNonRoot rather than an explicit numeric 
    runAsUser.

  RECOMMENDATION
    1) Upgrade containerd to >= 1.7.32, 2.0.9, 2.2.4, or 2.3.1 
    (containerd 2.1.x is end-of-life with no fix — migrate off it). 2) 
    Enforce a specific numeric runAsUser in the Pod securityContext, 
    which overrides the image USER directive and prevents the bypass. 3) 
    Kubernetes >= 1.34 enforces runAsNonRoot correctly regardless of this 
    bug. 4) Admit only trusted images; use OPA/Kyverno to reject UID 
    values >= 2147483647. 5) check_user_namespace_mapping (check 27) 
    flags running as UID 0 without userns remapping, the observable 
    end-state of this bypass. Interim mitigation (compensating control 
    until patched): Upgrade containerd to >= 1.7.32 / 2.0.9 / 2.2.4 / 
    2.3.1; enforce a numeric runAsUser in the Pod securityContext; use 
    Kubernetes >= 1.34; only run trusted images Patch status: no fixed 
    kernel version is recorded for this CVE (fixed_versions=none) — at 
    disclosure no upstream fix was available, or this is a 
    userspace/runtime CVE tracked separately. This finding cannot confirm 
    a patched kernel; verify against your distribution's security 
    advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2022_0492_kernel_cgroup_release_agent
  Severity     : HIGH
  Title        : cgroup release_agent Escape (CVE-2022-0492) — HIGH [ITW] [CISA-KEV]

  WHAT IT IS
    CVE: CVE-2022-0492. Name: cgroup release_agent Escape. CVSS: 7.0. 
    Kernel: 6.17.0-40-generic. Detection context: manual/advisory 
    tracking entry — no kernel version/module test applies; version 
    signal: not-affected (ubuntu/24.04 flavour=generic (running 
    6.17.0-40-generic) has no distro-specific tracking data, but running 
    series 6.17 is a full mainline release series past the highest fix 
    boundary (5.16.6, series 5.16) for this CVE — kernel.org mainline 
    releases are strictly cumulative, so 6.17 necessarily contains this 
    fix regardless of distro packaging (this differs from same-series 
    version-number proximity, which stays heuristic-only and unpromoted) 
    — treated as not-affected on that basis rather than left unknown. 
    If this distro backports fixes highly unusually (e.g. deliberately 
    reverting an upstream commit), that would be exceptional and should 
    be independently verified; this is not blanket confirmation of every 
    CVE at once, only this one.). ITW: yes. Public PoC: yes. CISA KEV: 
    yes. Subsystem: kernel/cgroup release_agent. Modules: none. 
    CVE-2022-0492 is an improper-authentication flaw in the Linux kernel 
    cgroup v1 release_agent feature. cgroup_release_agent_write() did not 
    verify that the writing process had the privileges normally required 
    to set the release_agent program. A process with CAP_SYS_ADMIN — or 
    one able to gain it within a new user+cgroup namespace on 
    misconfigured systems — could set the cgroup v1 release_agent to an 
    attacker-controlled program, which the kernel then executes on the 
    host (outside all container namespaces) when the last task in a 
    cgroup exits.

  CHECKS PERFORMED
    [pass] Version signal (supporting): ubuntu/24.04 flavour=generic 
        (running 6.17.0-40-generic) has no distro-specific tracking data, but 
        running series 6.17 is a full mainline release series past the highest 
        fix boundary (5.16.6, series 5.16) for this CVE — kernel.org mainline 
        releases are strictly cumulative, so 6.17 necessarily contains this fix 
        regardless of distro packaging (this differs from same-series 
        version-number proximity, which stays heuristic-only and unpromoted) — 
        treated as not-affected on that basis rather than left unknown. If this 
        distro backports fixes highly unusually (e.g. deliberately reverting an 
        upstream commit), that would be exceptional and should be independently 
        verified; this is not blanket confirmation of every CVE at once, only 
        this one. — kernel carries the fix/hardening; exploitability depends on 
        the behavioural precondition only
    [skip] Automated detection: primary detection is behavioural / 
        component-version based; tracked as advisory at severity HIGH — confirm 
        via dedicated check / installed component version

  IMPACT
    Container escape to host code execution as root. Where a container 
    has CAP_SYS_ADMIN and a writable cgroup v1 hierarchy (or can create 
    the necessary namespaces), the release_agent mechanism yields full 
    host compromise with no memory-corruption exploit required. Listed in 
    the CISA Known Exploited Vulnerabilities catalog.

  EXPLOITABILITY
    High and well-tooled. The release_agent escape is implemented as a 
    one-click technique in CDK and deepce and is one of the most widely 
    documented container escapes. Requires CAP_SYS_ADMIN (or an 
    equivalent path to it) and a writable cgroup v1 release_agent.

  RECOMMENDATION
    1) Migrate workloads to cgroup v2, which has no release_agent. 2) 
    Mount cgroupfs read-only inside containers. 3) Drop CAP_SYS_ADMIN 
    from application containers. 4) Apply seccomp to block mount(2) and 
    unshare(2)/clone(2) with namespace flags. 5) Enforce Pod Security 
    Admission at 'restricted'. The live detection is 
    check_cgroup_release_agent (check 12), which flags a writable 
    release_agent directly. Interim mitigation (compensating control 
    until patched): Migrate to cgroup v2 (no release_agent); mount 
    cgroupfs read-only; drop CAP_SYS_ADMIN; block mount(2) via seccomp 
    Patch status: no fixed kernel version is recorded for this CVE 
    (fixed_versions=none) — at disclosure no upstream fix was 
    available, or this is a userspace/runtime CVE tracked separately. 
    This finding cannot confirm a patched kernel; verify against your 
    distribution's security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_43494_net_rds
  Severity     : HIGH
  Title        : PinTheft (RDS zerocopy + io_uring page-cache overwrite) (CVE-2026-43494) — HIGH

  WHAT IT IS
    CVE: CVE-2026-43494. Name: PinTheft (RDS zerocopy + io_uring 
    page-cache overwrite). CVSS: 7.8. Kernel: 6.17.0-40-generic. 
    Detection context: kernel 6.17.0-40-generic version unknown for this 
    distro/release — no matching distro_status/vendor_defer row; 
    treated conservatively as affected, NOT a confirmed range hit; 
    modules not currently loaded but not blacklisted — auto-load on 
    socket creation is possible: rds rds_tcp. ITW: no. Public PoC: yes. 
    CISA KEV: no. Subsystem: net/rds. Modules: rds rds_tcp. A 
    reference-count flaw in the Linux kernel RDS (Reliable Datagram 
    Sockets) zerocopy send path. When rds_message_zcopy_from_user() pins 
    user pages and a later page faults, the error path drops pages it 
    already pinned while op_nents is not reset, so subsequent RDS message 
    cleanup drops the same references again. Each failed zerocopy send 
    steals one pin reference from the first page. The public exploit 
    chains this with io_uring fixed buffers (REGISTER_BUFFERS): after the 
    pin references are drained, io_uring still believes it holds a valid 
    page, but that physical page can be freed and reused by the page 
    cache, yielding a controlled overwrite of the in-memory page cache of 
    a SUID-root binary. Same end primitive as Copy Fail (CVE-2026-31431) 
    but reached through a different subsystem. The underlying RDS bug 
    dates to kernel 4.17 (2018); the working PoC additionally depends on 
    io_uring features present in roughly 6.6+.

  CHECKS PERFORMED
    [info] Version verdict (enriched): no matching fixed-version data for 
        ubuntu/24.04 flavour=generic (running 6.17.0-40-generic); cannot assert 
        patched — verify against vendor tracker. Heuristic only (base kernel 
        version vs NVD upstream ranges, NOT distro-backport-aware): vanilla 
        6.17.0 in NVD affected range [6.13, 6.18.33) — upstream fix at 6.18.33. 
        Distributions routinely backport fixes without changing the upstream 
        version string, so this heuristic can be wrong in either direction — it 
        is supporting context only, never a substitute for the vendor advisory.
    [pass] Module loaded (/proc/modules): none of [rds rds_tcp] present 
        in /proc/modules
    [FLAG] Module blacklist (/etc/modprobe.d): NOT blacklisted: rds 
        rds_tcp — auto-load on use remains possible
    [FLAG] Severity synthesis: kernel affected; modules not loaded but 
        not blacklisted (rds rds_tcp) — auto-load possible -> HIGH

  IMPACT
    Any unprivileged local user on an affected host gains root by 
    overwriting a setuid binary in the page cache. The on-disk file is 
    unchanged, so on-disk integrity checks miss it. The page cache is 
    shared across the host, so on multi-tenant nodes, CI runners, and 
    container hosts this is also a container-escape / node-compromise 
    vector. Two independent prerequisites must both hold: the rds/rds_tcp 
    modules must be loadable AND io_uring must be reachable.

  EXPLOITABILITY
    Reliable and deterministic. A public PoC and technical write-up (V12 
    Security / Aaron Esau) are available; no race-win retries needed once 
    both prerequisites are met. Requires only an unprivileged local 
    shell. Mitigated by default on distributions that blacklist RDS or 
    disable io_uring, which is why default Ubuntu/CloudLinux configs are 
    reported safe.

  RECOMMENDATION
    Apply the distribution kernel update that backports the upstream RDS 
    zerocopy fix (candidate posted to netdev 2026-05-05). Confirm the 
    fixed package version against your distribution's security tracker 
    before relying on a patched verdict, as backports vary by vendor. 
    Live detection is supported by the dangerous-modules audit (check 47, 
    rds/rds_tcp) and the io_uring reachability probe (check 48); a 
    finding is most actionable when both gates are present. Interim 
    mitigation (compensating control until patched): If RDS is not in use 
    (it rarely is outside Oracle RAC/InfiniBand HPC clusters), blacklist 
    the modules: printf 'install rds /bin/false\ninstall rds_tcp 
    /bin/false\n' > /etc/modprobe.d/pintheft.conf && rmmod rds_tcp rds 
    2>/dev/null. On kernels 6.6+ also gate the second half of the chain 
    by disabling io_uring with sysctl kernel.io_uring_disabled=2. Either 
    control alone breaks the exploit; many distros (e.g. Ubuntu) already 
    blacklist RDS by default via 
    /etc/modprobe.d/blacklist-rare-network.conf. Patch status: the fixed 
    kernel version is NOT yet confirmed in the CVE database 
    (fixed_versions=VERIFY). This host is flagged conservatively as 
    potentially affected; the engine cannot determine whether your kernel 
    is patched. Confirm the fixed package version against your 
    distribution's security tracker before treating this as resolved.

  ------------------------------------------------------------

  ID           : cve_cve_2026_46331_net_sched_act_pedit
  Severity     : HIGH
  Title        : pedit COW (CVE-2026-46331) — HIGH

  WHAT IT IS
    CVE: CVE-2026-46331. Name: pedit COW. CVSS: 8.5. Kernel: 
    6.17.0-40-generic. Detection context: kernel 6.17.0-40-generic 
    version unknown for this distro/release — no matching 
    distro_status/vendor_defer row; treated conservatively as affected, 
    NOT a confirmed range hit; modules not currently loaded but not 
    blacklisted — auto-load on socket creation is possible: act_pedit. 
    ITW: no. Public PoC: yes. CISA KEV: no. Subsystem: 
    net/sched/act_pedit. Modules: act_pedit. CVE-2026-46331 (pedit COW) 
    is a partial copy-on-write page-cache corruption flaw in the Linux 
    kernel net/sched traffic-control subsystem, in the act_pedit action 
    used by tc to rewrite packet header bytes in flight. tcf_pedit_act() 
    calls skb_ensure_writable() a single time before the key loop, 
    deriving the writable range from tcfp_off_max_hint — a static hint 
    that omits the runtime header offset added when a typed key resolves 
    its target at execution time. Writes inside the loop can therefore 
    land outside the COW'd region, mutating bytes in a page still shared 
    with the page cache. The upstream fix moves skb_ensure_writable() 
    inside the per-key loop, adds overflow checking on the offset 
    arithmetic, uses skb_cow() for negative (e.g. ingress Ethernet) 
    offsets, and guards offset_valid() against INT_MIN. Introduced by 
    commit 899ee91156e5; fix commits 899ee91156e5 2bec122b9fb9 
    3dee9d0c198f b198ed4e5258.

  CHECKS PERFORMED
    [info] Version verdict (enriched): no matching fixed-version data for 
        ubuntu/24.04 flavour=generic (running 6.17.0-40-generic); cannot assert 
        patched — verify against vendor tracker. Distributions routinely 
        backport fixes without changing the upstream version string, so this 
        heuristic can be wrong in either direction — it is supporting context 
        only, never a substitute for the vendor advisory.
    [pass] Module loaded (/proc/modules): none of [act_pedit] present in 
        /proc/modules
    [FLAG] Module blacklist (/etc/modprobe.d): NOT blacklisted: act_pedit 
        — auto-load on use remains possible
    [FLAG] Severity synthesis: kernel affected; modules not loaded but 
        not blacklisted (act_pedit) — auto-load possible -> HIGH

  IMPACT
    Any unprivileged local user gains a root shell on affected systems 
    where the act_pedit action is reachable. The public PoC poisons the 
    page-cached image of setuid-root /bin/su (overwriting its ELF entry 
    point with setgid(0)+setuid(0)+execve("/bin/sh") shellcode) and walks 
    straight to root. As with the other page-cache-poisoning bugs, the 
    on-disk binary is unchanged so file-integrity tooling does not detect 
    tampering, and the shared page cache makes this a container-escape / 
    node-compromise vector on multi-tenant hosts, Kubernetes nodes, and 
    CI/CD runners.

  EXPLOITABILITY
    High in practice despite the bug surfacing as a "routine 
    data-corruption" netdev patch with no security framing. A public, 
    author-verified PoC ("packet_edit_meme") appeared on GitHub within 24 
    hours of CVE assignment on 2026-06-16. Exploitation needs 
    CAP_NET_ADMIN to configure the tc pedit rule — but this is 
    reachable by unprivileged users via namespace-local CAP_NET_ADMIN 
    where unprivileged user namespaces are enabled (default on 
    Debian/Ubuntu). Deterministic page-cache write, no race. This is an 
    N-day: exploitable detail was on a public mailing list before most 
    teams had a CVE, advisory, or patch.

  RECOMMENDATION
    1) Install the patched distribution kernel and REBOOT (upstream fixed 
    in v7.1-rc7; verify against your vendor advisory: RHEL 
    RHSA-2026:27288 (10) / 27789 (9) / 27353 (8), Debian DSA-6355-1 for 
    Trixie). Confirm with 'uname -r' and ensure container/CI/VM hosts run 
    the updated host kernel. 2) Interim: act_pedit auto-loads the moment 
    a pedit tc action is referenced, so a plain blacklist is insufficient 
    — use an install override: 'echo install act_pedit /bin/true > 
    /etc/modprobe.d/disable-act_pedit.conf' (plus 'rmmod act_pedit' if 
    currently loaded). Most hosts run no tc pedit rules and can disable 
    it safely. 3) Defence-in-depth: disable unprivileged user namespaces 
    ('kernel.unprivileged_userns_clone=0' on Debian/Ubuntu, 
    'user.max_user_namespaces=0' on RHEL) to remove the namespace-local 
    CAP_NET_ADMIN path — breaks rootless containers and browser 
    sandboxes. 4) Treat any host suspected of running the exploit as 
    fully compromised regardless of file-integrity results. Interim 
    mitigation (compensating control until patched): echo 'install 
    act_pedit /bin/true' > /etc/modprobe.d/disable-act_pedit.conf; rmmod 
    act_pedit 2>/dev/null Patch status: comparison was made against 
    recorded fixed versions (trixie:6.12.94-1 sid:7.0.13-1 
    mainline:7.1-rc7). Distributions often backport fixes without 
    changing the upstream version string, so confirm against your 
    distribution's security advisory.

  ------------------------------------------------------------
============================================================
 MEDIUM FINDINGS
============================================================

  ID           : no_new_privs_unset
  Severity     : MEDIUM
  Title        : no_new_privs bit is not set (privilege escalation via SUID/file caps possible)

  WHAT IT IS
    The no_new_privs process attribute is 0, meaning the kernel will 
    still honour set-user-ID/set-group-ID bits and file capabilities when 
    this process or its children call execve(2). This is the kernel 
    primitive behind the Kubernetes allowPrivilegeEscalation setting; a 
    value of 0 indicates allowPrivilegeEscalation was not set to false 
    (or was explicitly true).

  IMPACT
    If a SUID-root binary (e.g. a stray sudo, mount, or ping with the 
    SUID bit) or a file with file capabilities exists in the container 
    image, a non-root process can execute it to gain elevated privileges, 
    which is often the first rung of an escape chain that other findings 
    in this report can complete.

  EXPLOITABILITY
    Low on its own; a force-multiplier when combined with SUID binaries 
    (check 13) or a writable-auth-file / mount finding. It removes a 
    kernel-enforced barrier rather than being an exploit itself.

  RECOMMENDATION
    Set securityContext.allowPrivilegeEscalation: false on the container 
    (this sets no_new_privs=1). Pair with runAsNonRoot: true and a 
    dropped capability set, and strip SUID/SGID bits from image binaries 
    that do not need them.

  ------------------------------------------------------------

  ID           : apparmor_unconfined
  Severity     : MEDIUM
  Title        : AppArmor profile is not applied (unconfined)

  WHAT IT IS
    AppArmor applies mandatory access control based on profiles 
    restricting file access, capabilities, and network operations.

  IMPACT
    The Docker default AppArmor profile blocks writes to /proc/sys and 
    restricts mount operations. Without it, these path-based restrictions 
    are absent.

  EXPLOITABILITY
    Not a direct escape, but removes protection against file-based attack 
    paths.

  RECOMMENDATION
    Ensure the runtime applies an AppArmor profile. Apply profiles via 
    securityContext.appArmorProfile in Kubernetes.

  ------------------------------------------------------------

  ID           : suid__usr_libexec_camel-lock-helper-1.2
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/libexec/camel-lock-helper-1.2

  WHAT IT IS
    /usr/libexec/camel-lock-helper-1.2 has the setuid or setgid bit set 
    and executes with the permissions of its owner regardless of who runs 
    it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for 
    /usr/libexec/camel-lock-helper-1.2. Many common binaries (find, vim, 
    python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_sbin_postdrop
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/sbin/postdrop

  WHAT IT IS
    /usr/sbin/postdrop has the setuid or setgid bit set and executes with 
    the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/sbin/postdrop. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_sbin_unix_chkpwd
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/sbin/unix_chkpwd

  WHAT IT IS
    /usr/sbin/unix_chkpwd has the setuid or setgid bit set and executes 
    with the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/sbin/unix_chkpwd. Many 
    common binaries (find, vim, python, cp, bash) have documented 
    one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_sbin_postqueue
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/sbin/postqueue

  WHAT IT IS
    /usr/sbin/postqueue has the setuid or setgid bit set and executes 
    with the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/sbin/postqueue. Many 
    common binaries (find, vim, python, cp, bash) have documented 
    one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_sbin_pam_extrausers_chkpwd
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/sbin/pam_extrausers_chkpwd

  WHAT IT IS
    /usr/sbin/pam_extrausers_chkpwd has the setuid or setgid bit set and 
    executes with the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for 
    /usr/sbin/pam_extrausers_chkpwd. Many common binaries (find, vim, 
    python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_sbin_pppd
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/sbin/pppd

  WHAT IT IS
    /usr/sbin/pppd has the setuid or setgid bit set and executes with the 
    permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/sbin/pppd. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_dotlockfile
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/dotlockfile

  WHAT IT IS
    /usr/bin/dotlockfile has the setuid or setgid bit set and executes 
    with the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/dotlockfile. Many 
    common binaries (find, vim, python, cp, bash) have documented 
    one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_su
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/su

  WHAT IT IS
    /usr/bin/su has the setuid or setgid bit set and executes with the 
    permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/su. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_mount
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/mount

  WHAT IT IS
    /usr/bin/mount has the setuid or setgid bit set and executes with the 
    permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/mount. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_ssh-agent
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/ssh-agent

  WHAT IT IS
    /usr/bin/ssh-agent has the setuid or setgid bit set and executes with 
    the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/ssh-agent. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_chfn
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/chfn

  WHAT IT IS
    /usr/bin/chfn has the setuid or setgid bit set and executes with the 
    permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/chfn. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_crontab
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/crontab

  WHAT IT IS
    /usr/bin/crontab has the setuid or setgid bit set and executes with 
    the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/crontab. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_newgrp
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/newgrp

  WHAT IT IS
    /usr/bin/newgrp has the setuid or setgid bit set and executes with 
    the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/newgrp. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_gpasswd
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/gpasswd

  WHAT IT IS
    /usr/bin/gpasswd has the setuid or setgid bit set and executes with 
    the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/gpasswd. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_umount
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/umount

  WHAT IT IS
    /usr/bin/umount has the setuid or setgid bit set and executes with 
    the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/umount. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_chsh
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/chsh

  WHAT IT IS
    /usr/bin/chsh has the setuid or setgid bit set and executes with the 
    permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/chsh. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_chage
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/chage

  WHAT IT IS
    /usr/bin/chage has the setuid or setgid bit set and executes with the 
    permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/chage. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_expiry
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/expiry

  WHAT IT IS
    /usr/bin/expiry has the setuid or setgid bit set and executes with 
    the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/expiry. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_fusermount3
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/fusermount3

  WHAT IT IS
    /usr/bin/fusermount3 has the setuid or setgid bit set and executes 
    with the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/fusermount3. Many 
    common binaries (find, vim, python, cp, bash) have documented 
    one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_dotlock.mailutils
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/dotlock.mailutils

  WHAT IT IS
    /usr/bin/dotlock.mailutils has the setuid or setgid bit set and 
    executes with the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/dotlock.mailutils. 
    Many common binaries (find, vim, python, cp, bash) have documented 
    one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_sudo
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/sudo

  WHAT IT IS
    /usr/bin/sudo has the setuid or setgid bit set and executes with the 
    permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/sudo. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_passwd
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/passwd

  WHAT IT IS
    /usr/bin/passwd has the setuid or setgid bit set and executes with 
    the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/passwd. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_bin_pkexec
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/bin/pkexec

  WHAT IT IS
    /usr/bin/pkexec has the setuid or setgid bit set and executes with 
    the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/bin/pkexec. Many common 
    binaries (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_lib_polkit-1_polkit-agent-helper-1
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/lib/polkit-1/polkit-agent-helper-1

  WHAT IT IS
    /usr/lib/polkit-1/polkit-agent-helper-1 has the setuid or setgid bit 
    set and executes with the permissions of its owner regardless of who 
    runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for 
    /usr/lib/polkit-1/polkit-agent-helper-1. Many common binaries (find, 
    vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_lib_xorg_Xorg.wrap
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/lib/xorg/Xorg.wrap

  WHAT IT IS
    /usr/lib/xorg/Xorg.wrap has the setuid or setgid bit set and executes 
    with the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/lib/xorg/Xorg.wrap. Many 
    common binaries (find, vim, python, cp, bash) have documented 
    one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_lib_dbus-1.0_dbus-daemon-launch-helper
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/lib/dbus-1.0/dbus-daemon-launch-helper

  WHAT IT IS
    /usr/lib/dbus-1.0/dbus-daemon-launch-helper has the setuid or setgid 
    bit set and executes with the permissions of its owner regardless of 
    who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for 
    /usr/lib/dbus-1.0/dbus-daemon-launch-helper. Many common binaries 
    (find, vim, python, cp, bash) have documented one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : suid__usr_lib_openssh_ssh-keysign
  Severity     : MEDIUM
  Title        : SUID/SGID binary present: /usr/lib/openssh/ssh-keysign

  WHAT IT IS
    /usr/lib/openssh/ssh-keysign has the setuid or setgid bit set and 
    executes with the permissions of its owner regardless of who runs it.

  IMPACT
    If exploitable via GTFOBins technique, command injection, or library 
    loading flaw, an attacker gains a root shell.

  EXPLOITABILITY
    Check https://gtfobins.github.io/ for /usr/lib/openssh/ssh-keysign. 
    Many common binaries (find, vim, python, cp, bash) have documented 
    one-liners.

  RECOMMENDATION
    Remove unnecessary SUID/SGID bits. Set no-new-privileges: true. Use 
    readOnlyRootFilesystem. Integrate SUID scanning into CI/CD.

  ------------------------------------------------------------

  ID           : escape_tool_nsenter
  Severity     : MEDIUM
  Title        : Namespace/runtime escape tool present: nsenter

  WHAT IT IS
    nsenter is a Linux utility for namespace manipulation, container 
    runtime interaction, or filesystem pivoting.

  IMPACT
    'nsenter -t 1 -m -u -i -n -p -- /bin/bash' enters the host's 
    namespaces directly when run with appropriate capabilities.

  EXPLOITABILITY
    Low standalone, High when combined with relevant capabilities.

  RECOMMENDATION
    Use minimal base images (distroless, scratch). Remove all tools not 
    required by the application.

  ------------------------------------------------------------

  ID           : escape_tool_unshare
  Severity     : MEDIUM
  Title        : Namespace/runtime escape tool present: unshare

  WHAT IT IS
    unshare is a Linux utility for namespace manipulation, container 
    runtime interaction, or filesystem pivoting.

  IMPACT
    'nsenter -t 1 -m -u -i -n -p -- /bin/bash' enters the host's 
    namespaces directly when run with appropriate capabilities.

  EXPLOITABILITY
    Low standalone, High when combined with relevant capabilities.

  RECOMMENDATION
    Use minimal base images (distroless, scratch). Remove all tools not 
    required by the application.

  ------------------------------------------------------------

  ID           : escape_tool_runc
  Severity     : MEDIUM
  Title        : Namespace/runtime escape tool present: runc

  WHAT IT IS
    runc is a Linux utility for namespace manipulation, container runtime 
    interaction, or filesystem pivoting.

  IMPACT
    'nsenter -t 1 -m -u -i -n -p -- /bin/bash' enters the host's 
    namespaces directly when run with appropriate capabilities.

  EXPLOITABILITY
    Low standalone, High when combined with relevant capabilities.

  RECOMMENDATION
    Use minimal base images (distroless, scratch). Remove all tools not 
    required by the application.

  ------------------------------------------------------------

  ID           : escape_tool_ctr
  Severity     : MEDIUM
  Title        : Namespace/runtime escape tool present: ctr

  WHAT IT IS
    ctr is a Linux utility for namespace manipulation, container runtime 
    interaction, or filesystem pivoting.

  IMPACT
    'nsenter -t 1 -m -u -i -n -p -- /bin/bash' enters the host's 
    namespaces directly when run with appropriate capabilities.

  EXPLOITABILITY
    Low standalone, High when combined with relevant capabilities.

  RECOMMENDATION
    Use minimal base images (distroless, scratch). Remove all tools not 
    required by the application.

  ------------------------------------------------------------

  ID           : escape_tool_chroot
  Severity     : MEDIUM
  Title        : Namespace/runtime escape tool present: chroot

  WHAT IT IS
    chroot is a Linux utility for namespace manipulation, container 
    runtime interaction, or filesystem pivoting.

  IMPACT
    'nsenter -t 1 -m -u -i -n -p -- /bin/bash' enters the host's 
    namespaces directly when run with appropriate capabilities.

  EXPLOITABILITY
    Low standalone, High when combined with relevant capabilities.

  RECOMMENDATION
    Use minimal base images (distroless, scratch). Remove all tools not 
    required by the application.

  ------------------------------------------------------------

  ID           : escape_tool_pivot_root
  Severity     : MEDIUM
  Title        : Namespace/runtime escape tool present: pivot_root

  WHAT IT IS
    pivot_root is a Linux utility for namespace manipulation, container 
    runtime interaction, or filesystem pivoting.

  IMPACT
    'nsenter -t 1 -m -u -i -n -p -- /bin/bash' enters the host's 
    namespaces directly when run with appropriate capabilities.

  EXPLOITABILITY
    Low standalone, High when combined with relevant capabilities.

  RECOMMENDATION
    Use minimal base images (distroless, scratch). Remove all tools not 
    required by the application.

  ------------------------------------------------------------

  ID           : debugfs_exposed
  Severity     : MEDIUM
  Title        : debugfs mounted and readable (/sys/kernel/debug)

  WHAT IT IS
    The Linux kernel debug filesystem (debugfs) is mounted and accessible 
    at /sys/kernel/debug. Access mode: readable. tracefs also mounted: 
    true.

  IMPACT
    Accessible debugfs provides: (1) Kernel tracing via ftrace — 
    capture arguments including memory contents and cryptographic 
    material from all host processes. (2) x86/pat_memtype_list exposes 
    memory type information. (3) Driver-specific debug interfaces may 
    expose hardware state or DMA buffers.

  EXPLOITABILITY
    Moderate. Simply reading ftrace ring buffer content can passively 
    capture sensitive data from host processes. Risk increases 
    significantly with write access.

  RECOMMENDATION
    Do not mount debugfs in production containers. If required, mount 
    read-only. Ensure /sys/kernel/debug is not included in any volume 
    mounts.

  ------------------------------------------------------------

  ID           : kernel_keyring_visible
  Severity     : MEDIUM
  Title        : Kernel keys visible in /proc/keys (4 keys)

  WHAT IT IS
    /proc/keys is readable and shows 4 keys. Without CAP_SYS_ADMIN the 
    keys themselves cannot generally be read, but metadata is visible.

  IMPACT
    Key metadata may reveal what encryption or authentication material is 
    stored, informing further attack planning.

  EXPLOITABILITY
    Low — metadata only without elevated capabilities.

  RECOMMENDATION
    Audit key permissions with keyctl show. Apply seccomp to block 
    keyctl(2) if not required.

  ------------------------------------------------------------

  ID           : kh_kptr_restrict_1
  Severity     : MEDIUM
  Title        : kptr_restrict=1: kernel pointers visible to root (recommend 2)

  WHAT IT IS
    kernel.kptr_restrict=1. Kernel symbol addresses are hidden from 
    unprivileged users but still visible to root-level processes (UID 0 
    or CAP_SYSLOG). Current value: 1. Recommended: 2.

  IMPACT
    Kernel Virtual Address Space Layout Randomisation (KASLR) is a 
    primary exploit mitigation. kptr_restrict=1 means that any 
    root-equivalent process — including processes in a privileged 
    container — can read kernel pointer values from /proc/kallsyms, 
    /proc/modules, and sysfs. This provides the KASLR bypass that is step 
    one of almost every modern kernel exploitation chain. An attacker who 
    has achieved UID 0 inside a container (e.g. via setuid binary, 
    capability escalation, or this script's check 7) can immediately 
    proceed to dereferencing kernel symbols.

  EXPLOITABILITY
    Low-moderate standalone. Combined with any kernel bug, the kernel 
    pointer leak removes the address-space randomisation barrier. This is 
    consistently the most valuable piece of information in a kernel 
    exploitation attempt.

  RECOMMENDATION
    Set kernel.kptr_restrict=2 in /etc/sysctl.d/99-hardening.conf on the 
    host. This is non-breaking for all standard workloads and is included 
    in the CIS Benchmark for Linux, DISA STIG, and ANSSI hardening guides.

  ------------------------------------------------------------

  ID           : kh_ipv4_all_send_redirects
  Severity     : MEDIUM
  Title        : ipv4/conf/all/send_redirects=1: host emitting ICMP redirects

  WHAT IT IS
    Kernel parameter: net.ipv4.conf.all.send_redirects. Current value: 1. 
    Recommended: 0. The host will generate and send ICMP redirect 
    messages to clients, informing them of better routes. This behaviour 
    can be abused to redirect traffic from legitimate clients.

  IMPACT
    An attacker who achieves code execution on the host can leverage 
    send_redirects to poison the routing caches of connected clients, 
    redirecting their traffic through an attacker-controlled host. In 
    shared network segments this affects all pods on the same subnet.

  EXPLOITABILITY
    Low standalone. Relevant as a post-exploitation tool to facilitate 
    lateral movement.

  RECOMMENDATION
    Set net.ipv4.conf.all.send_redirects=0 in /etc/sysctl.d/.

  ------------------------------------------------------------

  ID           : kh_ipv4_default_send_redirects
  Severity     : MEDIUM
  Title        : ipv4/conf/default/send_redirects=1: host emitting ICMP redirects

  WHAT IT IS
    Kernel parameter: net.ipv4.conf.default.send_redirects. Current 
    value: 1. Recommended: 0. The host will generate and send ICMP 
    redirect messages to clients, informing them of better routes. This 
    behaviour can be abused to redirect traffic from legitimate clients.

  IMPACT
    An attacker who achieves code execution on the host can leverage 
    send_redirects to poison the routing caches of connected clients, 
    redirecting their traffic through an attacker-controlled host. In 
    shared network segments this affects all pods on the same subnet.

  EXPLOITABILITY
    Low standalone. Relevant as a post-exploitation tool to facilitate 
    lateral movement.

  RECOMMENDATION
    Set net.ipv4.conf.all.send_redirects=0 in /etc/sysctl.d/.

  ------------------------------------------------------------

  ID           : kh_ipv4_default_source_route
  Severity     : MEDIUM
  Title        : ipv4/conf/default/accept_source_route=1: source routing accepted

  WHAT IT IS
    Kernel parameter: net.ipv4.conf.default.accept_source_route. Current 
    value: 1. Recommended: 0. The host honours IP source routing options 
    in packet headers, allowing senders to specify the exact route their 
    packets take through the network.

  IMPACT
    Source routing bypasses network-level access controls and firewalls 
    by specifying a path that avoids filtering devices. An attacker can 
    route packets through intermediaries that would normally be 
    unreachable, enabling access to services protected by network 
    topology.

  EXPLOITABILITY
    Low standalone. Primarily a pivot and evasion technique in 
    post-exploitation.

  RECOMMENDATION
    Set net.ipv4.conf.all.accept_source_route=0 in /etc/sysctl.d/.

  ------------------------------------------------------------

  ID           : kh_mod_bluetooth
  Severity     : MEDIUM
  Title        : Dangerous kernel module loaded: bluetooth

  WHAT IT IS
    Kernel module 'bluetooth' is currently loaded (visible in 
    /proc/modules). Reason: Bluetooth subsystem — large attack surface 
    (BlueFrag, BIAS, KNOB class attacks); almost never legitimate in 
    server/container workloads. Associated CVE(s): none.

  IMPACT
    Module-specific attack surface is active and exploitable. See the 
    reason field above. For modules with active ITW CVEs (algif_aead, 
    esp4, esp6, rxrpc), the host is exposed to current in-the-wild 
    exploits providing local privilege escalation and container escape.

  EXPLOITABILITY
    Module is already loaded — no auto-load trigger needed. Exploit 
    primitives (sockets, syscalls) are immediately accessible. Attack 
    complexity depends on the specific module — see reasons above.

  RECOMMENDATION
    Blacklist: 'install bluetooth /bin/false; install btusb /bin/false'. 
    Server workloads have no legitimate use for Bluetooth.

  ------------------------------------------------------------

  ID           : cve_cve_2026_46333_kernel_ptrace
  Severity     : MEDIUM
  Title        : Ptrace Credential Hijack (CVE-2026-46333) — patch status unknown for this system

  WHAT IT IS
    CVE: CVE-2026-46333. No fixed-version data in the database matched 
    distro=ubuntu/24.04 flavour=generic (kernel 6.17.0-40-generic), so 
    patch status cannot be asserted.

  CHECKS PERFORMED
    [info] Version verdict (enriched): no matching fixed-version data for 
        ubuntu/24.04 flavour=generic (running 6.17.0-40-generic); cannot assert 
        patched — verify against vendor tracker. Heuristic only (base kernel 
        version vs NVD upstream ranges, NOT distro-backport-aware): vanilla 
        6.17.0 in NVD affected range [6.13, 6.18.31) — upstream fix at 6.18.31. 
        Distributions routinely backport fixes without changing the upstream 
        version string, so this heuristic can be wrong in either direction — it 
        is supporting context only, never a substitute for the vendor advisory.

  IMPACT
    Undetermined — treated as potentially affected rather than silently 
    passed. Verify against the vendor tracker.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Confirm exposure to CVE-2026-46333 using your distribution's security 
    tracker and the installed kernel package version.

  ------------------------------------------------------------

  ID           : cve_cve_2026_43121_io_uring_zcrx
  Severity     : MEDIUM
  Title        : io_uring zcrx Freelist OOB (CVE-2026-43121) — patch status unknown for this system

  WHAT IT IS
    CVE: CVE-2026-43121. No fixed-version data in the database matched 
    distro=ubuntu/24.04 flavour=generic (kernel 6.17.0-40-generic), so 
    patch status cannot be asserted.

  CHECKS PERFORMED
    [info] Version verdict (enriched): no matching fixed-version data for 
        ubuntu/24.04 flavour=generic (running 6.17.0-40-generic); cannot assert 
        patched — verify against vendor tracker. Heuristic only (base kernel 
        version vs NVD upstream ranges, NOT distro-backport-aware): vanilla 
        6.17.0 in NVD affected range [6.15, 6.18.16) — upstream fix at 6.18.16. 
        Distributions routinely backport fixes without changing the upstream 
        version string, so this heuristic can be wrong in either direction — it 
        is supporting context only, never a substitute for the vendor advisory.

  IMPACT
    Undetermined — treated as potentially affected rather than silently 
    passed. Verify against the vendor tracker.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Confirm exposure to CVE-2026-43121 using your distribution's security 
    tracker and the installed kernel package version.

  ------------------------------------------------------------

  ID           : cve_cve_2026_55686_podman_libpod_workdir_creation
  Severity     : MEDIUM
  Title        : Podman WORKDIR Symlink Host Write (CVE-2026-55686) — MEDIUM

  WHAT IT IS
    CVE: CVE-2026-55686. Name: Podman WORKDIR Symlink Host Write. CVSS: 
    5.3. Kernel: 6.17.0-40-generic. Detection context: manual/advisory 
    tracking entry — no kernel version/module test applies; affected 
    component: podman; fixed in podman: VERIFY. ITW: no. Public PoC: no. 
    CISA KEV: no. Subsystem: podman/libpod workdir creation. Modules: 
    none. CVE-2026-55686 is a symlink-following flaw in Podman/libpod. 
    Running a malicious container image whose WORKDIR path contains a 
    symlink can cause Podman, during host-side working-directory 
    creation, to create a directory at the symlink target on the host 
    filesystem. Ownership of an existing host path can also be modified, 
    though that variant additionally requires a race with an 
    untrusted/malicious process mutating the host filesystem tree during 
    dereferencing of the WORKDIR path.

  CHECKS PERFORMED
    [skip] Automated detection: primary detection is behavioural / 
        component-version based; tracked as advisory at severity MEDIUM; 
        component=podman; component fixed=VERIFY — confirm via dedicated check 
        / installed component version

  IMPACT
    Limited host filesystem integrity impact: attacker-influenced 
    directory creation on the host, and (race-dependent) ownership 
    changes of host paths. The attacker controls the target path but not 
    arbitrary file contents, bounding the severity. Same general class as 
    the runc /dev symlink and masked-path races.

  EXPLOITABILITY
    Low. Directory creation is reliable given a crafted image; the 
    ownership-modification variant needs a host-side race and is 
    described as less likely. No public weaponised PoC. CVSS ~5.3.

  RECOMMENDATION
    1) Upgrade Podman/libpod to the fixed release (confirm version from 
    GHSA-q6r4-3wmg-fwcq). 2) Run only trusted images; treat images with 
    symlinked WORKDIR as suspicious. 3) The behavioural class is covered 
    by check_runc_masked_path (symlink/masked-path races) and by the 
    writable-core_pattern / sensitive-mount checks. Interim mitigation 
    (compensating control until patched): Upgrade Podman/libpod to the 
    fixed release; only run trusted images; avoid images with symlinked 
    WORKDIR Patch status: no fixed kernel version is recorded for this 
    CVE (fixed_versions=none) — at disclosure no upstream fix was 
    available, or this is a userspace/runtime CVE tracked separately. 
    This finding cannot confirm a patched kernel; verify against your 
    distribution's security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_41579_opencontainers_runc_rootfs_setup_(setupPtmx_setupDevSymlinks)
  Severity     : MEDIUM
  Title        : runc /dev Symlink Limited Host Write (CVE-2026-41579) — MEDIUM

  WHAT IT IS
    CVE: CVE-2026-41579. Name: runc /dev Symlink Limited Host Write. 
    CVSS: 3.3. Kernel: 6.17.0-40-generic. Detection context: 
    manual/advisory tracking entry — no kernel version/module test 
    applies; affected component: runc; fixed in runc: 1.4.3. ITW: no. 
    Public PoC: no. CISA KEV: no. Subsystem: opencontainers/runc rootfs 
    setup (setupPtmx/setupDevSymlinks). Modules: none. CVE-2026-41579 is 
    a low-severity flaw in runc's container rootfs preparation. When 
    setting up the container rootfs, setupPtmx and setupDevSymlinks 
    create /dev symlinks; a malicious image supplying a crafted /dev 
    symlink could obtain limited write access to the host filesystem. It 
    is the same class of bug as the November 2025 runc 
    masked-path/symlink CVEs (CVE-2025-31133, CVE-2025-52565, 
    CVE-2025-52881) and was missed when that rootfs-preparation hardening 
    was done.

  CHECKS PERFORMED
    [skip] Automated detection: primary detection is behavioural / 
        component-version based; tracked as advisory at severity MEDIUM; 
        component=runc; component fixed=1.4.3 — confirm via dedicated check / 
        installed component version

  IMPACT
    Limited and, per runc's own analysis, too constrained to be 
    problematic in practice — the write access is narrow rather than an 
    arbitrary host-file overwrite. Still a defence-in-depth regression in 
    the /dev setup path. Container-escape relevance is bounded.

  EXPLOITABILITY
    Low. Reported by multiple people; released publicly with no embargo 
    precisely because severity is low. No weaponised PoC. CVSS 3.3.

  RECOMMENDATION
    1) Update runc to >= 1.4.3 or >= 1.5.0-rc3 (1.5.0 expected shortly 
    after). Ensure containerd/Docker use the patched runc. 2) Run only 
    trusted images. 3) check_runc_masked_path detects the broader 
    vulnerable-runc-version condition and the related masked-path CVEs. 
    Interim mitigation (compensating control until patched): Upgrade runc 
    to >= 1.4.3 or >= 1.5.0-rc3 (then 1.5.0); only run trusted images 
    Patch status: no fixed kernel version is recorded for this CVE 
    (fixed_versions=none) — at disclosure no upstream fix was 
    available, or this is a userspace/runtime CVE tracked separately. 
    This finding cannot confirm a patched kernel; verify against your 
    distribution's security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_53359_arch_x86_kvm_(shadow_MMU,_shared_Intel_AMD_code_path)
  Severity     : MEDIUM
  Title        : Januscape (CVE-2026-53359) — patch status must be confirmed via vendor advisory

  WHAT IT IS
    CVE: CVE-2026-53359. The running distribution backports fixes, so the 
    kernel version string alone cannot determine whether this CVE is 
    patched. Distro=ubuntu/24.04, kernel=6.17.0-40-generic.

  CHECKS PERFORMED
    [info] Architecture gate: CVE requires x86_64 (normalised x86_64); 
        host x86_64 (normalised x86_64) matches — proceeding
    [info] Version verdict (enriched): ubuntu: consult USN-pending — 
        backport status not derivable from version string

  IMPACT
    Undetermined from version alone — do not assume patched. Verify 
    against the vendor security advisory for this CVE.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Check your distribution's advisory for CVE-2026-53359 and confirm the 
    installed kernel package meets or exceeds the fixed build.

  ------------------------------------------------------------

  ID           : cve_cve_2026_46113_arch_x86_kvm_(shadow_MMU,_shared_Intel_AMD_code_path_—_same_subsystem_as_CVE-2026-53359)
  Severity     : MEDIUM
  Title        : KVM Shadow-MMU UAF (Januscape companion) (CVE-2026-46113) — patch status unknown for this system

  WHAT IT IS
    CVE: CVE-2026-46113. No fixed-version data in the database matched 
    distro=ubuntu/24.04 flavour=generic (kernel 6.17.0-40-generic), so 
    patch status cannot be asserted.

  CHECKS PERFORMED
    [info] Architecture gate: CVE requires x86_64 (normalised x86_64); 
        host x86_64 (normalised x86_64) matches — proceeding
    [info] Version verdict (enriched): no matching fixed-version data for 
        ubuntu/24.04 flavour=generic (running 6.17.0-40-generic); cannot assert 
        patched — verify against vendor tracker. Heuristic only (base kernel 
        version vs NVD upstream ranges, NOT distro-backport-aware): vanilla 
        6.17.0 outside all NVD affected ranges (patched-or-never-affected under 
        closed-world model). Distributions routinely backport fixes without 
        changing the upstream version string, so this heuristic can be wrong in 
        either direction — it is supporting context only, never a substitute 
        for the vendor advisory.

  IMPACT
    Undetermined — treated as potentially affected rather than silently 
    passed. Verify against the vendor tracker.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Confirm exposure to CVE-2026-46113 using your distribution's security 
    tracker and the installed kernel package version.

  ------------------------------------------------------------

  ID           : cve_cve_2026_53362_net_ipv6_(__ip6_append_data,_paged_fragment_assembly)
  Severity     : MEDIUM
  Title        : IPv6 Fragmentation Container Escape (CVE-2026-53362) — patch status unknown for this system

  WHAT IT IS
    CVE: CVE-2026-53362. No fixed-version data in the database matched 
    distro=ubuntu/24.04 flavour=generic (kernel 6.17.0-40-generic), so 
    patch status cannot be asserted.

  CHECKS PERFORMED
    [info] Version verdict (enriched): no matching fixed-version data for 
        ubuntu/24.04 flavour=generic (running 6.17.0-40-generic); cannot assert 
        patched — verify against vendor tracker. Heuristic only (base kernel 
        version vs NVD upstream ranges, NOT distro-backport-aware): vanilla 
        6.17.0 outside all NVD affected ranges (patched-or-never-affected under 
        closed-world model). Distributions routinely backport fixes without 
        changing the upstream version string, so this heuristic can be wrong in 
        either direction — it is supporting context only, never a substitute 
        for the vendor advisory.

  IMPACT
    Undetermined — treated as potentially affected rather than silently 
    passed. Verify against the vendor tracker.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Confirm exposure to CVE-2026-53362 using your distribution's security 
    tracker and the installed kernel package version.

  ------------------------------------------------------------

  ID           : cve_cve_2026_46242_fs_eventpoll_(epoll_ep_remove()_cleanup_path)
  Severity     : MEDIUM
  Title        : Bad Epoll (CVE-2026-46242) — patch status unknown for this system

  WHAT IT IS
    CVE: CVE-2026-46242. No fixed-version data in the database matched 
    distro=ubuntu/24.04 flavour=generic (kernel 6.17.0-40-generic), so 
    patch status cannot be asserted.

  CHECKS PERFORMED
    [info] Version verdict (enriched): no matching fixed-version data for 
        ubuntu/24.04 flavour=generic (running 6.17.0-40-generic); cannot assert 
        patched — verify against vendor tracker. Heuristic only (base kernel 
        version vs NVD upstream ranges, NOT distro-backport-aware): vanilla 
        6.17.0 outside all NVD affected ranges (patched-or-never-affected under 
        closed-world model). Distributions routinely backport fixes without 
        changing the upstream version string, so this heuristic can be wrong in 
        either direction — it is supporting context only, never a substitute 
        for the vendor advisory.

  IMPACT
    Undetermined — treated as potentially affected rather than silently 
    passed. Verify against the vendor tracker.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Confirm exposure to CVE-2026-46242 using your distribution's security 
    tracker and the installed kernel package version.

  ------------------------------------------------------------

  ID           : cve_cve_2026_43499_kernel_futex_(priority-inheritance_futex_handling)
  Severity     : MEDIUM
  Title        : GhostLock (CVE-2026-43499) — patch status unknown for this system

  WHAT IT IS
    CVE: CVE-2026-43499. No fixed-version data in the database matched 
    distro=ubuntu/24.04 flavour=generic (kernel 6.17.0-40-generic), so 
    patch status cannot be asserted.

  CHECKS PERFORMED
    [info] Version verdict (enriched): no matching fixed-version data for 
        ubuntu/24.04 flavour=generic (running 6.17.0-40-generic); cannot assert 
        patched — verify against vendor tracker. Heuristic only (base kernel 
        version vs NVD upstream ranges, NOT distro-backport-aware): vanilla 
        6.17.0 outside all NVD affected ranges (patched-or-never-affected under 
        closed-world model). Distributions routinely backport fixes without 
        changing the upstream version string, so this heuristic can be wrong in 
        either direction — it is supporting context only, never a substitute 
        for the vendor advisory.

  IMPACT
    Undetermined — treated as potentially affected rather than silently 
    passed. Verify against the vendor tracker.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Confirm exposure to CVE-2026-43499 using your distribution's security 
    tracker and the installed kernel package version.

  ------------------------------------------------------------

  ID           : cve_cve_2026_23268_security_apparmor_(LSM)
  Severity     : MEDIUM
  Title        : CrackArmor AppArmor Confused Deputy (CVE-2026-23268) — patch status must be confirmed via vendor advisory

  WHAT IT IS
    CVE: CVE-2026-23268. The running distribution backports fixes, so the 
    kernel version string alone cannot determine whether this CVE is 
    patched. Distro=ubuntu/24.04, kernel=6.17.0-40-generic.

  CHECKS PERFORMED
    [info] Version verdict (enriched): ubuntu: consult 
        ubuntu.com/security/vulnerabilities/crackarmor — backport status not 
        derivable from version string

  IMPACT
    Undetermined from version alone — do not assume patched. Verify 
    against the vendor security advisory for this CVE.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Check your distribution's advisory for CVE-2026-23268 and confirm the 
    installed kernel package meets or exceeds the fixed build.

  ------------------------------------------------------------
============================================================
 INFO FINDINGS
============================================================

  ID           : kernel_version
  Severity     : INFO
  Title        : Kernel version: 6.17.0-40-generic

  WHAT IT IS
    The host kernel version is 6.17.0-40-generic. Detailed CVE version 
    checks are performed by the config-driven CVE engine (see CVE check 
    section below).

  IMPACT
    Outdated kernels may be vulnerable to container escape CVEs 
    exploitable by unprivileged users inside containers.

  EXPLOITABILITY
    uname -r is available to any user. Version information is sufficient 
    to identify applicable CVEs.

  RECOMMENDATION
    Keep the host kernel patched. Use a container-optimised OS with 
    automated security updates. See CVE check findings for specific 
    vulnerabilities.

  ------------------------------------------------------------

  ID           : modules_loading_enabled
  Severity     : INFO
  Title        : Kernel module loading is enabled (modules_disabled=0)

  WHAT IT IS
    modules_disabled=0 means kernel modules can be loaded at runtime. 
    Combined with CAP_SYS_MODULE, this permits loading arbitrary kernel 
    code.

  IMPACT
    If CAP_SYS_MODULE is also present, a malicious .ko module can be 
    loaded to establish persistence, spawn reverse shells, or disable 
    audit logging.

  EXPLOITABILITY
    High if CAP_SYS_MODULE is present; informational otherwise.

  RECOMMENDATION
    Set kernel.modules_disabled=1 via sysctl after all necessary modules 
    are loaded at boot.

  ------------------------------------------------------------

  ID           : cve_2025_31133_runc_masked
  Severity     : INFO
  Title        : runc masked path CVEs — version 1.3.6 appears patched

  WHAT IT IS
    runc 1.3.6 is present and appears to be >= 1.2.8/1.3.3 (patched). 
    core_pattern writable: false.

  IMPACT
    N/A — runc version appears patched.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Verify with 'runc --version'. Ensure containerd/Docker are also 
    updated to use the patched runc.

  ------------------------------------------------------------

  ID           : kh_ipv4_forwarding_enabled
  Severity     : INFO
  Title        : net.ipv4.ip_forward=1 — IPv4 forwarding enabled on host

  WHAT IT IS
    The host kernel is configured to forward IPv4 packets between network 
    interfaces (net.ipv4.ip_forward=1). This is required and expected on 
    Kubernetes worker nodes for CNI overlay networking. However, the 
    presence of IP forwarding means the node will route packets between 
    container and host networks.

  IMPACT
    If a container has CAP_NET_ADMIN (see check 2) or access to the host 
    network namespace, IP forwarding can be used to route traffic through 
    the host node, bypassing NetworkPolicy controls and enabling 
    man-in-the-middle positioning on the pod network. Even without extra 
    capabilities, overly permissive CNI configuration combined with 
    forwarding can allow containers to reach node-internal services.

  EXPLOITABILITY
    Low standalone. Meaningful when combined with CAP_NET_ADMIN or host 
    network namespace access (see checks 2 and 3).

  RECOMMENDATION
    Verify CNI NetworkPolicy is enforced and that inter-pod traffic is 
    filtered at the CNI level. Ensure iptables/nftables rules on the node 
    restrict unexpected forwarding paths. This parameter should not be 
    changed on Kubernetes nodes.

  ------------------------------------------------------------

  ID           : cve_2026_34040_docker_authz_binary
  Severity     : INFO
  Title        : dockerd 29.6.1 — patched for CVE-2026-34040

  WHAT IT IS
    dockerd binary reachable at /usr/bin/dockerd reports version 29.6.1, 
    at or above the fixed 29.3.1.

  IMPACT
    N/A — version appears patched.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    N/A

  ------------------------------------------------------------

  ID           : cve_cve_2024_1086_net_netfilter_nf_tables
  Severity     : INFO
  Title        : Flipping Pages (CVE-2024-1086) — kernel 6.17.0-40-generic appears not affected (not-affected)

  WHAT IT IS
    CVE: CVE-2024-1086. Compound check: kernel version 
    verdict=not-affected (not in affected range / at or above fix). 
    Module status: loaded=nf_tables, not-blacklisted=nf_tables. Socket 
    accessible: false.

  CHECKS PERFORMED
    [pass] Version verdict (enriched): ubuntu/24.04 flavour=generic 
        (running 6.17.0-40-generic) has no distro-specific tracking data, but 
        running series 6.17 is a full mainline release series past the highest 
        fix boundary (6.7.3, series 6.7) for this CVE — kernel.org mainline 
        releases are strictly cumulative, so 6.17 necessarily contains this fix 
        regardless of distro packaging (this differs from same-series 
        version-number proximity, which stays heuristic-only and unpromoted) — 
        treated as not-affected on that basis rather than left unknown. If this 
        distro backports fixes highly unusually (e.g. deliberately reverting an 
        upstream commit), that would be exceptional and should be independently 
        verified; this is not blanket confirmation of every CVE at once, only 
        this one.
    [FLAG] Module loaded (/proc/modules): LOADED: nf_tables
    [FLAG] Module blacklist (/etc/modprobe.d): NOT blacklisted: nf_tables 
        — auto-load on use remains possible
    [pass] Severity synthesis: kernel not affected (not-affected) -> INFO 
        (not vulnerable on version)

  IMPACT
    N/A — kernel version check passed.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Verify with distribution advisory. Continue applying kernel updates.

  ------------------------------------------------------------

  ID           : cve_cve_2025_21756_net_vmw_vsock
  Severity     : INFO
  Title        : Attack of the Vsock (CVE-2025-21756) — kernel 6.17.0-40-generic appears not affected (not-affected)

  WHAT IT IS
    CVE: CVE-2025-21756. Compound check: kernel version 
    verdict=not-affected (not in affected range / at or above fix). 
    Module status: loaded=vmw_vsock_vmci_transport vsock, 
    not-blacklisted=vmw_vsock_vmci_transport vsock. Socket accessible: 
    true.

  CHECKS PERFORMED
    [pass] Version verdict (enriched): ubuntu/24.04 flavour=generic 
        (running 6.17.0-40-generic) has no distro-specific tracking data, but 
        running series 6.17 is a full mainline release series past the highest 
        fix boundary (6.13.4, series 6.13) for this CVE — kernel.org mainline 
        releases are strictly cumulative, so 6.17 necessarily contains this fix 
        regardless of distro packaging (this differs from same-series 
        version-number proximity, which stays heuristic-only and unpromoted) — 
        treated as not-affected on that basis rather than left unknown. If this 
        distro backports fixes highly unusually (e.g. deliberately reverting an 
        upstream commit), that would be exceptional and should be independently 
        verified; this is not blanket confirmation of every CVE at once, only 
        this one.
    [FLAG] Module loaded (/proc/modules): LOADED: 
        vmw_vsock_vmci_transport vsock
    [FLAG] Module blacklist (/etc/modprobe.d): NOT blacklisted: 
        vmw_vsock_vmci_transport vsock — auto-load on use remains possible
    [FLAG] Socket reachability: AF=40 SOCK=1 PROTO=0: opened cleanly (AF 
        reachable)
    [pass] Severity synthesis: kernel not affected (not-affected) -> INFO 
        (not vulnerable on version)

  IMPACT
    N/A — kernel version check passed.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Verify with distribution advisory. Continue applying kernel updates.

  ------------------------------------------------------------

  ID           : cve_cve_2025_38352_kernel_time_posix-cpu-timers
  Severity     : INFO
  Title        : Chronomaly (CVE-2025-38352) — kernel 6.17.0-40-generic appears patched (patched)

  WHAT IT IS
    CVE: CVE-2025-38352. Verdict: patched. Distro=ubuntu/24.04 
    flavour=generic, kernel 6.17.0-40-generic.

  CHECKS PERFORMED
    [info] Introduced-version gate: running 6.17.0 (int 6017000) >= 
        introduced 2.6.36 (int 2006036) — at/after the version that introduced 
        the bug
    [pass] Fixed-version comparison: series 6.17 not listed in 
        fixed_versions (5.15:5.15.190 6.1:6.1.144 6.6:6.6.97 6.12:6.12.34 
        mainline:6.16.0); newer than highest fixed series 6.12 — fix presumed 
        in mainline, not affected

  IMPACT
    N/A — kernel version check passed.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Continue to apply kernel updates. Verify with your distribution's 
    security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2025_38617_net_packet
  Severity     : INFO
  Title        : Packet Socket Race (CVE-2025-38617) — kernel 6.17.0-40-generic appears not affected (not-affected)

  WHAT IT IS
    CVE: CVE-2025-38617. Compound check: kernel version 
    verdict=not-affected (not in affected range / at or above fix). 
    Module status: loaded=none, not-blacklisted=none. Socket accessible: 
    true.

  CHECKS PERFORMED
    [pass] Version verdict (enriched): ubuntu/24.04 flavour=generic 
        (running 6.17.0-40-generic) has no distro-specific tracking data, but 
        running series 6.17 is a full mainline release series past the highest 
        fix boundary (6.16.1, series 6.16) for this CVE — kernel.org mainline 
        releases are strictly cumulative, so 6.17 necessarily contains this fix 
        regardless of distro packaging (this differs from same-series 
        version-number proximity, which stays heuristic-only and unpromoted) — 
        treated as not-affected on that basis rather than left unknown. If this 
        distro backports fixes highly unusually (e.g. deliberately reverting an 
        upstream commit), that would be exceptional and should be independently 
        verified; this is not blanket confirmation of every CVE at once, only 
        this one.
    [FLAG] Socket reachability: AF=17 SOCK=3 PROTO=0: errno 1 (EPERM) — 
        AF reachable
    [pass] Severity synthesis: kernel not affected (not-affected) -> INFO 
        (not vulnerable on version)

  IMPACT
    N/A — kernel version check passed.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Verify with distribution advisory. Continue applying kernel updates.

  ------------------------------------------------------------

  ID           : cve_cve_2023_0386_fs_overlayfs
  Severity     : INFO
  Title        : OverlayFS SetUID Copy-Up (FUSE) Privilege Escalation (CVE-2023-0386) — kernel 6.17.0-40-generic appears patched (not-affected)

  WHAT IT IS
    CVE: CVE-2023-0386. Verdict: not-affected. Distro=ubuntu/24.04 
    flavour=generic, kernel 6.17.0-40-generic.

  CHECKS PERFORMED
    [pass] Version verdict (enriched): ubuntu/24.04 flavour=generic 
        (running 6.17.0-40-generic) has no distro-specific tracking data, but 
        running series 6.17 is a full mainline release series past the highest 
        fix boundary (6.1.9, series 6.1) for this CVE — kernel.org mainline 
        releases are strictly cumulative, so 6.17 necessarily contains this fix 
        regardless of distro packaging (this differs from same-series 
        version-number proximity, which stays heuristic-only and unpromoted) — 
        treated as not-affected on that basis rather than left unknown. If this 
        distro backports fixes highly unusually (e.g. deliberately reverting an 
        upstream commit), that would be exceptional and should be independently 
        verified; this is not blanket confirmation of every CVE at once, only 
        this one.

  IMPACT
    N/A — kernel version check passed.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Continue to apply kernel updates. Verify with your distribution's 
    security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2022_0847_fs_pipe
  Severity     : INFO
  Title        : DirtyPipe (CVE-2022-0847) — kernel 6.17.0-40-generic appears patched (not-affected)

  WHAT IT IS
    CVE: CVE-2022-0847. Verdict: not-affected. Distro=ubuntu/24.04 
    flavour=generic, kernel 6.17.0-40-generic.

  CHECKS PERFORMED
    [pass] Version verdict (enriched): ubuntu/24.04 flavour=generic 
        (running 6.17.0-40-generic) has no distro-specific tracking data, but 
        running series 6.17 is a full mainline release series past the highest 
        fix boundary (5.16.11, series 5.16) for this CVE — kernel.org mainline 
        releases are strictly cumulative, so 6.17 necessarily contains this fix 
        regardless of distro packaging (this differs from same-series 
        version-number proximity, which stays heuristic-only and unpromoted) — 
        treated as not-affected on that basis rather than left unknown. If this 
        distro backports fixes highly unusually (e.g. deliberately reverting an 
        upstream commit), that would be exceptional and should be independently 
        verified; this is not blanket confirmation of every CVE at once, only 
        this one.

  IMPACT
    N/A — kernel version check passed.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Continue to apply kernel updates. Verify with your distribution's 
    security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2016_5195_mm_gup
  Severity     : INFO
  Title        : DirtyCOW (CVE-2016-5195) — kernel 6.17.0-40-generic appears patched (not-affected)

  WHAT IT IS
    CVE: CVE-2016-5195. Verdict: not-affected. Distro=ubuntu/24.04 
    flavour=generic, kernel 6.17.0-40-generic.

  CHECKS PERFORMED
    [pass] Version verdict (enriched): ubuntu/24.04 flavour=generic 
        (running 6.17.0-40-generic) has no distro-specific tracking data, but 
        running series 6.17 is a full mainline release series past the highest 
        fix boundary (4.8.3, series 4.8) for this CVE — kernel.org mainline 
        releases are strictly cumulative, so 6.17 necessarily contains this fix 
        regardless of distro packaging (this differs from same-series 
        version-number proximity, which stays heuristic-only and unpromoted) — 
        treated as not-affected on that basis rather than left unknown. If this 
        distro backports fixes highly unusually (e.g. deliberately reverting an 
        upstream commit), that would be exceptional and should be independently 
        verified; this is not blanket confirmation of every CVE at once, only 
        this one.

  IMPACT
    N/A — kernel version check passed.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Continue to apply kernel updates. Verify with your distribution's 
    security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_23111_net_netfilter_nf_tables
  Severity     : INFO
  Title        : nf_tables Catchall Verdict-Map UAF (CVE-2026-23111) — kernel 6.17.0-40-generic appears not affected (fixed)

  WHAT IT IS
    CVE: CVE-2026-23111. Compound check: kernel version verdict=fixed 
    (not in affected range / at or above fix). Module status: 
    loaded=nf_tables, not-blacklisted=nf_tables. Socket accessible: false.

  CHECKS PERFORMED
    [pass] Version verdict (enriched): running 6.17.0-40.40~24.04.1 >= 
        fixed 6.8.0-107.107 on ubuntu/24.04/generic
    [FLAG] Module loaded (/proc/modules): LOADED: nf_tables
    [FLAG] Module blacklist (/etc/modprobe.d): NOT blacklisted: nf_tables 
        — auto-load on use remains possible
    [pass] Severity synthesis: kernel not affected (fixed) -> INFO (not 
        vulnerable on version)

  IMPACT
    N/A — kernel version check passed.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Verify with distribution advisory. Continue applying kernel updates.

  ------------------------------------------------------------

  ID           : cve_cve_2026_43503_net_core_skbuff
  Severity     : INFO
  Title        : DirtyClone (CVE-2026-43503) — kernel 6.17.0-40-generic appears not affected (fixed)

  WHAT IT IS
    CVE: CVE-2026-43503. Compound check: kernel version verdict=fixed 
    (not in affected range / at or above fix). Module status: 
    loaded=none, not-blacklisted=none. Socket accessible: false.

  CHECKS PERFORMED
    [pass] Version verdict (enriched): running 6.17.0-40.40~24.04.1 >= 
        fixed 6.8.0-124.124 on ubuntu/24.04/generic
    [pass] Module loaded (/proc/modules): none of [esp4 esp6 rxrpc] 
        present in /proc/modules
    [pass] Module blacklist (/etc/modprobe.d): all of [esp4 esp6 rxrpc] 
        blacklisted via 'install ... /bin/false'
    [pass] Severity synthesis: kernel not affected (fixed) -> INFO (not 
        vulnerable on version)

  IMPACT
    N/A — kernel version check passed.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Verify with distribution advisory. Continue applying kernel updates.

  ------------------------------------------------------------

  ID           : cve_cve_2026_46316_arch_arm64_kvm_vgic
  Severity     : INFO
  Title        : ITScape (CVE-2026-46316) — not applicable on x86_64 (requires arm64)

  WHAT IT IS
    CVE: CVE-2026-46316. This vulnerability affects the arm64 
    architecture only. The host reports x86_64 (uname -m), so the 
    kernel-version/module/socket test is not applicable and was skipped 
    to avoid a false positive on kernel version alone.

  CHECKS PERFORMED
    [skip] Architecture gate: CVE requires arm64 (normalised arm64); host 
        is x86_64 (normalised x86_64) — not applicable, version/module tests 
        skipped

  IMPACT
    N/A — wrong architecture.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    No action required on this architecture. If you also operate arm64 
    hosts, run the audit there and apply the vendor patch: 1) Apply a 
    kernel containing mainline fix commit 13031fb6b835 (and the related 
    CVE-2026-46317 / follow-up commits). On Rocky/RLC: update to the 
    patched RLC Pro LTS 9.6 / RLC Pro 9 / RLC Pro 10 kernels. 2) There is 
    no drop-in software mitigation — the exploitable code is the 
    emulated ITS inside the arm64 KVM vGIC, reached from a guest. 3) 
    Until patched, restrict arm64 KVM hosts to guests and tenants you 
    trust; prioritise multi-tenant arm64 virtualization for patching. 4) 
    Cloud tenants on arm64 hardware should confirm patch status with 
    their provider..

  ------------------------------------------------------------

  ID           : cve_cve_2022_47939_fs_ksmbd
  Severity     : INFO
  Title        : ksmbd Tree-Disconnect UAF (CVE-2022-47939) — kernel 6.17.0-40-generic appears not affected (not-affected)

  WHAT IT IS
    CVE: CVE-2022-47939. Compound check: kernel version 
    verdict=not-affected (not in affected range / at or above fix). 
    Module status: loaded=none, not-blacklisted=ksmbd. Socket accessible: 
    false.

  CHECKS PERFORMED
    [pass] Version verdict (enriched): ubuntu/24.04 flavour=generic 
        (running 6.17.0-40-generic) has no distro-specific tracking data, but 
        running series 6.17 is a full mainline release series past the highest 
        fix boundary (5.19.2, series 5.19) for this CVE — kernel.org mainline 
        releases are strictly cumulative, so 6.17 necessarily contains this fix 
        regardless of distro packaging (this differs from same-series 
        version-number proximity, which stays heuristic-only and unpromoted) — 
        treated as not-affected on that basis rather than left unknown. If this 
        distro backports fixes highly unusually (e.g. deliberately reverting an 
        upstream commit), that would be exceptional and should be independently 
        verified; this is not blanket confirmation of every CVE at once, only 
        this one.
    [pass] Module loaded (/proc/modules): none of [ksmbd] present in 
        /proc/modules
    [FLAG] Module blacklist (/etc/modprobe.d): NOT blacklisted: ksmbd — 
        auto-load on use remains possible
    [pass] Severity synthesis: kernel not affected (not-affected) -> INFO 
        (not vulnerable on version)

  IMPACT
    N/A — kernel version check passed.

  EXPLOITABILITY
    N/A

  RECOMMENDATION
    Verify with distribution advisory. Continue applying kernel updates.

  ------------------------------------------------------------

  ID           : cve_cve_2019_5736_opencontainers_runc_process_exec_(_proc_self_exe_file-descriptor_mishandling)
  Severity     : INFO
  Title        : runc /proc/self/exe Host Binary Overwrite (CVE-2019-5736) — INFO [ITW] [CISA-KEV]

  WHAT IT IS
    CVE: CVE-2019-5736. Name: runc /proc/self/exe Host Binary Overwrite. 
    CVSS: 8.6. Kernel: 6.17.0-40-generic. Detection context: runc 1.3.6 
    detected at /usr/bin/runc (via check 52) — at or above the 1.0-rc7 
    fix for this CVE. Confirmed not vulnerable, not merely 'no signal'.. 
    ITW: yes. Public PoC: yes. CISA KEV: yes. Subsystem: 
    opencontainers/runc process exec (/proc/self/exe file-descriptor 
    mishandling). Modules: none. CVE-2019-5736 is the original runc 
    container escape. Because runc did not open its own executable with 
    O_CLOEXEC, a container process running as root can open 
    /proc/self/exe (a magic-link back to the host runc binary), then use 
    the leaked, still-open file descriptor to overwrite the host runc 
    binary from inside the container. The bug is triggered either by (1) 
    a malicious image the host operator runs via `runc run`, or (2) an 
    attacker with write access inside an existing container that is later 
    attached with `runc exec`. Once the host runc binary is replaced with 
    attacker code, the next container start/exec executes that code as 
    root on the host.

  CHECKS PERFORMED
    [pass] Check 52 cross-reference: runc 1.3.6 detected at 
        /usr/bin/runc; at or above runc's fix (1.0-rc7)

  IMPACT
    Full container-to-host escape yielding host root. Affects every 
    runtime that embeds vulnerable runc (Docker, containerd, CRI-O, 
    Kubernetes nodes, and derivatives). On multi-tenant nodes and CI/CD 
    build farms a single malicious image or one writable container is 
    enough to compromise the entire host and, by extension, every other 
    workload on it.

  EXPLOITABILITY
    Trivial and fully weaponised. Public PoCs and a Metasploit module 
    exist; listed in CISA KEV as exploited in the wild. Requires only the 
    ability to run root inside a container (the default) plus either a 
    malicious image or `exec` access to a container. CVSS v3.x 8.6. 
    Exploitation preconditions ARE observable from inside a container 
    even though the runc version is not: it needs in-container root that 
    maps to host root (no userns remapping) and a writable path to the 
    host runc binary. Absence of those preconditions is meaningful 
    negative evidence.

  RECOMMENDATION
    Definitive fix: upgrade runc to >= 1.0-rc7 and ensure the host's 
    Docker/containerd/CRI-O ship the patched binary (Docker >= 18.09.2); 
    confirm with `runc --version` ON THE HOST. Because the host runc 
    version is normally NOT readable from inside a container, this entry 
    uses a three-state verdict instead of a version compare: (a) 
    VERIFY-ON-HOST — if the host runc version is obtainable (e.g. a 
    mounted runtime socket via check 31, or host-side inspection), 
    compare it directly; (b) POTENTIALLY EXPOSED — if check 27 shows 
    in-container root with NO userns remapping (/proc/self/uid_map maps 0 
    -> 0), the escape precondition is present, so flag and tell the 
    operator to verify host runc; (c) HARDENED AGAINST THIS CLASS — if 
    userns remapping is active (in-container root maps to an unprivileged 
    host UID), the binary-overwrite fails regardless of runc version, so 
    downgrade to INFO. The uid_map / remapping signal is already gathered 
    by check 27; check 26 (runc masked-path) surfaces any runc version 
    string that is reachable. Treat "no signal" as UNKNOWN, never as 
    SAFE. Interim mitigation (compensating control until patched): 
    Strongest single control is user-namespace remapping: if in-container 
    root maps to an unprivileged host UID (check /proc/self/uid_map — a 
    first entry that does NOT map container 0 to host 0 means remapped), 
    the container process cannot overwrite the root-owned host runc 
    binary and this specific escape fails regardless of runc version. 
    Additional defence-in-depth: run only trusted images; avoid `exec` 
    into untrusted running containers; where the runtime supports it, use 
    a read-only/dmz-protected runc (runc >= 1.1.12 self-copies via 
    memfd). None of these substitute for upgrading runc to >= 1.0-rc7 
    (Docker >= 18.09.2), which is the actual fix. Patch status: no fixed 
    kernel version is recorded for this CVE (fixed_versions=none) — at 
    disclosure no upstream fix was available, or this is a 
    userspace/runtime CVE tracked separately. This finding cannot confirm 
    a patched kernel; verify against your distribution's security 
    advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2024_21626_opencontainers_runc_process_cwd_handling_(internal_file-descriptor_leak)
  Severity     : INFO
  Title        : runc process.cwd Leaked-FD Container Breakout (CVE-2024-21626) — INFO

  WHAT IT IS
    CVE: CVE-2024-21626. Name: runc process.cwd Leaked-FD Container 
    Breakout. CVSS: 8.6. Kernel: 6.17.0-40-generic. Detection context: 
    runc 1.3.6 detected at /usr/bin/runc (via check 52) — at or above 
    the 1.1.12 fix for this CVE. Confirmed not vulnerable, not merely 'no 
    signal'.. ITW: no. Public PoC: yes. CISA KEV: no. Subsystem: 
    opencontainers/runc process.cwd handling (internal file-descriptor 
    leak). Modules: none. CVE-2024-21626 (Leaky Vessels) is an 
    order-of-operations container breakout in runc <= 1.1.11. An internal 
    file-descriptor leak lets a newly spawned container process inherit a 
    working directory (process.cwd) that resolves into the host 
    filesystem namespace. A malicious image can trigger this on `runc 
    run` / `docker build` (including indirectly via FROM or ONBUILD), and 
    an attacker inside a container can trigger it via `runc exec` 
    ("attack 2"). Because runc did not verify that the final working 
    directory was inside the container mount namespace after chdir, the 
    container process gains access to the host filesystem; variants 
    ("attack 3a"/"3b") adapt this to overwrite semi-arbitrary host 
    binaries via /proc/self/fd/N/../.. path traversal for a complete 
    escape.

  CHECKS PERFORMED
    [pass] Check 52 cross-reference: runc 1.3.6 detected at 
        /usr/bin/runc; at or above runc's fix (1.1.12)

  IMPACT
    Container-to-host escape giving read/write access to the host 
    filesystem and, in the binary-overwrite variants, full host 
    compromise. In Docker/Kubernetes contexts it is effectively critical: 
    anyone able to start a container image, or to get a malicious image 
    built (supply-chain / compromised base image), can reach the node. 
    Multi-tenant nodes, shared build farms, and registries pulling 
    untrusted layers are the high-risk cases.

  EXPLOITABILITY
    Public PoCs and demos exist for both the malicious-image and 
    docker-build vectors; no in-the-wild KEV listing. Attack 1 / 3a need 
    a privileged user tricked into running a malicious image (UI:R), 
    while attacks 2 / 3b break out from inside a container with only 
    ordinary `exec` access. All four lead to host takeover. CVSS 8.6 
    (NVD); per-attack scores range roughly 7.2-8.2. Unlike most runc bugs 
    this one leaves a distinctive OBSERVABLE artefact: a 
    successfully-exploited process has a working directory outside the 
    container mount namespace, so its /proc/<pid>/cwd symlink resolves 
    onto the host filesystem. runc's own 1.1.12 fix is precisely a guard 
    that cwd is inside the container root.

  RECOMMENDATION
    Definitive fix: upgrade runc to >= 1.1.12 and rebuild/repull any 
    images and higher-level runtimes that bundle it; apply 
    managed-Kubernetes node updates per your provider; verify host `runc 
    --version`. Because the host runc version is normally NOT readable 
    from inside a container, this entry uses a three-state verdict: (a) 
    VERIFY-ON-HOST — compare directly if the host runc version is 
    reachable (mounted runtime socket via check 31, or host-side 
    inspection); (b) POTENTIALLY EXPOSED / ACTIVE-COMPROMISE SIGNAL — a 
    read-only cwd-escape probe: walk /proc/*/cwd (and check the auditor's 
    own cwd) and flag any whose target escapes the container root (e.g. 
    resolves to a path/device or st_dev/inode inconsistent with "/" of 
    the container rootfs, or contains /proc/self/fd/ traversal). A cwd 
    pointing outside the container root is strong evidence of an 
    in-progress or successful attack-2/3 breakout; c) LOW-SIGNAL — no 
    escaped cwd observed. Note (c) does NOT clear the CVE: it only means 
    no exploitation is currently visible; the fd leak can still be 
    triggered by a fresh malicious image/exec. Snyk's eBPF 
    leaky-vessels-runtime-detector is the higher-fidelity live detector 
    and static Dockerfile analysers (WORKDIR/ONBUILD inspection) catch 
    malicious images pre-run. check 26 surfaces any reachable runc 
    version string. Treat "no signal" as UNKNOWN, never SAFE. Interim 
    mitigation (compensating control until patched): Upgrade runc to >= 
    1.1.12 and update anything that bundles runc (Docker, containerd, 
    CRI-O, managed-Kubernetes node images per vendor advisories). 
    Interim/defence-in-depth: only build/run trusted images and treat 
    FROM/ONBUILD base images as untrusted input; disabling unprivileged 
    user namespaces (kernel.unprivileged_userns_clone=0 on Debian/Ubuntu, 
    user.max_user_namespaces=0 on RHEL) shrinks the surrounding 
    escalation surface though it does not fix the fd leak itself. Follow 
    cloud-provider node-patch advisories for managed clusters. Patch 
    status: no fixed kernel version is recorded for this CVE 
    (fixed_versions=none) — at disclosure no upstream fix was 
    available, or this is a userspace/runtime CVE tracked separately. 
    This finding cannot confirm a patched kernel; verify against your 
    distribution's security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_50195_containerd_CRI_checkpoint_import
  Severity     : INFO
  Title        : containerd CRI Checkpoint Image Cache Poisoning (CVE-2026-50195) — INFO

  WHAT IT IS
    CVE: CVE-2026-50195. Name: containerd CRI Checkpoint Image Cache 
    Poisoning. CVSS: 8.8. Kernel: 6.17.0-40-generic. Detection context: 
    containerd 2.2.6 detected at /usr/bin/containerd (via check 52) — 
    at or above the branch fix for this CVE. Confirmed not vulnerable, 
    not merely 'no signal'.. ITW: no. Public PoC: yes. CISA KEV: no. 
    Subsystem: containerd CRI checkpoint import. Modules: none. 
    CVE-2026-50195 is a missing-validation flaw in containerd's CRI 
    checkpoint import path. containerd does not validate the image 
    references specified inside a checkpoint image's configuration. An 
    attacker with permission to create pods supplies a crafted checkpoint 
    whose io.containerd.cri.image-ref annotation points to an 
    attacker-controlled image; on import containerd pulls that image and 
    assigns it an arbitrary local tag, poisoning the node's local image 
    cache so a legitimate tag now resolves to the attacker's image.

  CHECKS PERFORMED
    [pass] Check 52 cross-reference: containerd 2.2.6 detected at 
        /usr/bin/containerd; at or above the CRI-cluster branch fix

  IMPACT
    Cross-pod code execution on a shared Kubernetes node. Any other pod 
    on the same node that later uses the poisoned tag with an 
    IfNotPresent or Never pull policy silently runs the attacker's image 
    and executes code under the victim pod's identity — a 
    lateral-movement / privilege-escalation primitive across tenants on a 
    node, and a stepping stone to node/host compromise when chained with 
    an in-container escape. containerd underpins EKS, ECS, Fargate, 
    Bottlerocket and Amazon Linux, so managed platforms are in scope 
    until their node runtimes are patched.

  EXPLOITABILITY
    Preconditions: checkpoint/restore enabled on the node (CRIU 
    installed) and the attacker holding pod-create permission (or 
    equivalent CRI access). Given those, the mechanism is a 
    straightforward crafted checkpoint tar with a poisoned image-ref 
    annotation, imported via the CRI API or ctr — no memory-corruption 
    or race. CVSS 8.8.

  RECOMMENDATION
    1) Upgrade containerd to >= 2.3.2, 2.2.5, 2.1.9, 2.0.10, or 1.7.33 
    (apply your managed provider's patched node image where applicable 
    — AWS is rolling patched runtimes to EKS/ECS/Fargate/Bottlerocket). 
    2) If checkpoint/restore is not needed, do not install CRIU / disable 
    the feature — this removes this CVE's attack surface. 3) Tighten 
    RBAC so untrusted principals cannot create pods or import 
    checkpoints. 4) Admit only trusted images and registries; prefer 
    digest-pinned images and Always pull policies for sensitive workloads 
    to defeat local-tag poisoning. 5) check 52 (check_runtime_versions) 
    probes the reachable containerd version and now cross-references this 
    CVE — a version below the branch fix is reported as a definitive 
    verdict. Interim mitigation (compensating control until patched): 
    Upgrade containerd to >= 2.3.2 / 2.2.5 / 2.1.9 / 2.0.10 / 1.7.33; 
    disable checkpoint/restore (do not install CRIU) if unused; restrict 
    pod-create RBAC; only allow trusted images and registries Patch 
    status: no fixed kernel version is recorded for this CVE 
    (fixed_versions=none) — at disclosure no upstream fix was 
    available, or this is a userspace/runtime CVE tracked separately. 
    This finding cannot confirm a patched kernel; verify against your 
    distribution's security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_53488_containerd_CRI_image-config_LABEL_handling
  Severity     : INFO
  Title        : containerd Image LABEL Host Command Execution (CVE-2026-53488) — INFO

  WHAT IT IS
    CVE: CVE-2026-53488. Name: containerd Image LABEL Host Command 
    Execution. CVSS: 8.3. Kernel: 6.17.0-40-generic. Detection context: 
    containerd 2.2.6 detected at /usr/bin/containerd (via check 52) — 
    at or above the branch fix for this CVE. Confirmed not vulnerable, 
    not merely 'no signal'.. ITW: no. Public PoC: no. CISA KEV: no. 
    Subsystem: containerd CRI image-config LABEL handling. Modules: none. 
    CVE-2026-53488 is an input-sanitisation flaw in containerd's CRI 
    plugin: image configuration LABEL instructions are propagated to 
    containers without sanitisation, allowing a crafted container image 
    to trigger arbitrary command execution on the host. Unlike 
    CVE-2026-50195, exploitation does NOT depend on the 
    checkpoint/restore feature being enabled.

  CHECKS PERFORMED
    [pass] Check 52 cross-reference: containerd 2.2.6 detected at 
        /usr/bin/containerd; at or above the CRI-cluster branch fix

  IMPACT
    Arbitrary host command execution from a malicious image — a direct 
    container-to-host code-execution path, hence a 
    container-escape/node-compromise primitive. Broadest of the cluster 
    because it needs neither CRIU nor CDI; running an untrusted image is 
    sufficient.

  EXPLOITABILITY
    Requires running a crafted/untrusted image on an unpatched node. No 
    public PoC captured, but the mechanism (malicious LABEL propagated 
    unsanitised into a host-side command context) is straightforward. 
    CVSS 8.3. There is no workaround other than upgrading.

  RECOMMENDATION
    1) Upgrade containerd to >= 2.3.2 / 2.2.5 / 2.1.9 / 2.0.10 / 1.7.33 
    — no feature toggle mitigates this. 2) Strictly limit which images 
    may run (admission control, trusted registries, digest pinning). 3) 
    check 52 cross-references this CVE against the reachable containerd 
    version. Interim mitigation (compensating control until patched): 
    Upgrade containerd to >= 2.3.2 / 2.2.5 / 2.1.9 / 2.0.10 / 1.7.33; 
    only run trusted images. No feature toggle mitigates this one — 
    upgrading is the only fix. Patch status: no fixed kernel version is 
    recorded for this CVE (fixed_versions=none) — at disclosure no 
    upstream fix was available, or this is a userspace/runtime CVE 
    tracked separately. This finding cannot confirm a patched kernel; 
    verify against your distribution's security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_53492_containerd_CRI_CDI_annotation_handling
  Severity     : INFO
  Title        : containerd CDI Annotation Host-Mount Injection (CVE-2026-53492) — INFO

  WHAT IT IS
    CVE: CVE-2026-53492. Name: containerd CDI Annotation Host-Mount 
    Injection. CVSS: 6.8. Kernel: 6.17.0-40-generic. Detection context: 
    containerd 2.2.6 detected at /usr/bin/containerd (via check 52) — 
    at or above the branch fix for this CVE. Confirmed not vulnerable, 
    not merely 'no signal'.. ITW: no. Public PoC: no. CISA KEV: no. 
    Subsystem: containerd CRI CDI annotation handling. Modules: none. 
    CVE-2026-53492 is a trust flaw in containerd's CRI plugin: CDI 
    (Container Device Interface) annotations carried in untrusted 
    checkpoint image metadata are honoured without validation. A crafted 
    checkpoint image can smuggle CDI annotations that inject devices and 
    host mounts into the restored container, bypassing Kubernetes device 
    enforcement.

  CHECKS PERFORMED
    [pass] Check 52 cross-reference: containerd 2.2.6 detected at 
        /usr/bin/containerd; at or above the CRI-cluster branch fix

  IMPACT
    Attacker-controlled device and host-path mount injection into a 
    container, defeating Kubernetes device admission — a path toward 
    accessing host resources and escaping isolation. Gated on the node 
    having CDI enabled and on checkpoint/restore being available.

  EXPLOITABILITY
    Requires CDI enabled on the node plus the ability to import a crafted 
    checkpoint (pod-create / CRI access). CVSS 6.8. No public PoC 
    captured.

  RECOMMENDATION
    1) Upgrade containerd to >= 2.3.2 / 2.2.5 / 2.1.9 / 2.0.10 / 1.7.33. 
    2) Disable CDI support if not required; also disable 
    checkpoint/restore (CRIU) if unused — either removes this CVE's 
    attack surface. 3) Restrict pod-create/checkpoint-import RBAC and 
    admit only trusted images. 4) check 52 cross-references this CVE 
    against the reachable containerd version. Interim mitigation 
    (compensating control until patched): Upgrade containerd to >= 2.3.2 
    / 2.2.5 / 2.1.9 / 2.0.10 / 1.7.33; disable checkpoint/restore (CRIU) 
    and disable CDI support if unused; only run trusted images Patch 
    status: no fixed kernel version is recorded for this CVE 
    (fixed_versions=none) — at disclosure no upstream fix was 
    available, or this is a userspace/runtime CVE tracked separately. 
    This finding cannot confirm a patched kernel; verify against your 
    distribution's security advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_53489_containerd_CRI_checkpoint_restore_(symlink_handling)
  Severity     : INFO
  Title        : containerd Checkpoint-Restore Host File Read (CVE-2026-53489) — INFO

  WHAT IT IS
    CVE: CVE-2026-53489. Name: containerd Checkpoint-Restore Host File 
    Read. CVSS: VERIFY. Kernel: 6.17.0-40-generic. Detection context: 
    containerd 2.2.6 detected at /usr/bin/containerd (via check 52) — 
    at or above the branch fix for this CVE. Confirmed not vulnerable, 
    not merely 'no signal'.. ITW: no. Public PoC: no. CISA KEV: no. 
    Subsystem: containerd CRI checkpoint restore (symlink handling). 
    Modules: none. CVE-2026-53489 is a symlink-following flaw in 
    containerd's CRI checkpoint restore path: a symlink embedded in a 
    checkpoint can cause containerd to read a file at an attacker-chosen 
    location on the host during restore, disclosing arbitrary host file 
    contents.

  CHECKS PERFORMED
    [pass] Check 52 cross-reference: containerd 2.2.6 detected at 
        /usr/bin/containerd; at or above the CRI-cluster branch fix

  IMPACT
    Arbitrary host file read (e.g. secrets, tokens, keys) from within a 
    restore operation — an information-disclosure primitive that aids 
    further escalation. Gated on checkpoint/restore being enabled and the 
    attacker being able to import a checkpoint.

  EXPLOITABILITY
    Requires checkpoint/restore enabled (CRIU) and checkpoint-import 
    access. No public PoC captured; CVSS not yet confirmed.

  RECOMMENDATION
    1) Upgrade containerd to >= 2.3.2 / 2.2.5 / 2.1.9 / 2.0.10 / 1.7.33. 
    2) Disable checkpoint/restore if unused. 3) Restrict RBAC; admit only 
    trusted images. 4) check 52 cross-references this CVE against the 
    reachable containerd version. Interim mitigation (compensating 
    control until patched): Upgrade containerd to >= 2.3.2 / 2.2.5 / 
    2.1.9 / 2.0.10 / 1.7.33; disable checkpoint/restore (do not install 
    CRIU) if unused; only run trusted images Patch status: no fixed 
    kernel version is recorded for this CVE (fixed_versions=none) — at 
    disclosure no upstream fix was available, or this is a 
    userspace/runtime CVE tracked separately. This finding cannot confirm 
    a patched kernel; verify against your distribution's security 
    advisory.

  ------------------------------------------------------------

  ID           : cve_cve_2026_47262_containerd_CRI_image_handling_(resource_consumption)
  Severity     : INFO
  Title        : containerd Image-Triggered Runtime DoS (CVE-2026-47262) — INFO

  WHAT IT IS
    CVE: CVE-2026-47262. Name: containerd Image-Triggered Runtime DoS. 
    CVSS: VERIFY. Kernel: 6.17.0-40-generic. Detection context: 
    containerd 2.2.6 detected at /usr/bin/containerd (via check 52) — 
    at or above the branch fix for this CVE. Confirmed not vulnerable, 
    not merely 'no signal'.. ITW: no. Public PoC: no. CISA KEV: no. 
    Subsystem: containerd CRI image handling (resource consumption). 
    Modules: none. CVE-2026-47262 lets a crafted container image trigger 
    uncontrolled resource consumption in the containerd runtime, 
    resulting in denial of service on the affected node.

  CHECKS PERFORMED
    [pass] Check 52 cross-reference: containerd 2.2.6 detected at 
        /usr/bin/containerd; at or above the CRI-cluster branch fix

  IMPACT
    Availability only: a malicious image can degrade or crash the 
    container runtime on a node, disrupting co-located workloads. No 
    confidentiality/integrity impact by itself, but useful as a nuisance 
    or to mask other activity.

  EXPLOITABILITY
    Requires running a crafted/untrusted image on an unpatched node. CVSS 
    not yet confirmed; no public PoC captured. No workaround other than 
    upgrading.

  RECOMMENDATION
    1) Upgrade containerd to >= 2.3.2 / 2.2.5 / 2.1.9 / 2.0.10 / 1.7.33. 
    2) Limit which images may run. 3) check 52 cross-references this CVE 
    against the reachable containerd version. Interim mitigation 
    (compensating control until patched): Upgrade containerd to >= 2.3.2 
    / 2.2.5 / 2.1.9 / 2.0.10 / 1.7.33; only run trusted images. No 
    workaround other than upgrading. Patch status: no fixed kernel 
    version is recorded for this CVE (fixed_versions=none) — at 
    disclosure no upstream fix was available, or this is a 
    userspace/runtime CVE tracked separately. This finding cannot confirm 
    a patched kernel; verify against your distribution's security 
    advisory.

  ------------------------------------------------------------

END OF REPORT
This report was generated for authorised security assessment purposes only.
